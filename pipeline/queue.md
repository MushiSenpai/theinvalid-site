# Blog queue + lesson backlog — the single repository

Two sections. **§A** is the FIFO publication queue (the Wednesday bot takes the
FIRST `[queued]` block). **§B** is the comprehensive backlog: EVERY issue,
failure, and decision across ALL projects, past and future, with priority.
Topics get promoted §B → §A by writing a topic block. Every work session that
produces a lesson appends to §B (or straight to §A if it's clearly a post).
Format rules: PIPELINE.md.

---

# §A — Publication queue (FIFO)

## [published 2026-06-13] your-firewall-isnt-protecting-your-docker-containers
**Angle:** Docker silently bypasses UFW for every published port — found because a phone on my own Wi-Fi could open my ComfyUI with zero auth. The DOCKER-USER chain fix, and why you must verify from an untrusted device.
**Sources:** sovereign-ai-stack repo LESSONS.md (Docker bypasses UFW); scripts/harden-docker-firewall.sh; backlog I-2.
**Targets:** linkedin, reddit:r/selfhosted, hn

## [published 2026-06-19] a-32gb-gpu-is-a-budget-not-a-suggestion
**Angle:** The VRAM discipline that makes one RTX 5090 run an LLM stack, a video stack, and an audio stack: unplugging the monitor from the GPU to run display on onboard graphics (~25K context tokens reclaimed), never co-loading models — purge VRAM, fresh-load per task, sequential modes with handoff scripts — and why NVFP4 was the only quantization leaving real KV-cache headroom.
**Sources:** Decision Log §12 (sequential workflow), §13 (iGPU switch), §7 (180K ctx math), README VRAM tables; backlog S-1..S-5.
**Targets:** linkedin, reddit:r/LocalLLaMA, hn

## [published 2026-06-20] nightly-wheels-are-a-depreciating-asset
**Angle:** Rebuilding a one-month-old Dockerfile failed twice because "install from the nightly index" instructions expire in weeks; the working container had silently drifted to stable torch. Pin what's proven; runtime pip installs rot.
**Sources:** audio-stack repo LESSONS.md ("The June rebuild"); worker-pip-freeze-2026-06-10.txt; backlog I-4.
**Targets:** linkedin, reddit:r/LocalLLaMA

## [published 2026-06-24] moving-dockers-data-root-doesnt-move-containerd
**Angle:** /var hit 99% weeks after I "moved Docker to the big disk" — image builds live in containerd's store, a different root. How the watchdog caught it day-1, finding the eater, the migration script.
**Sources:** EXECUTION-PLAN 2026-06-11; scripts/move-containerd-root.sh; backlog I-3.
**Targets:** linkedin, reddit:r/selfhosted, hn

## [published 2026-06-27] six-hours-in-tensorrt-llm-so-you-dont-have-to
**Angle:** Eight distinct failures ending at AutoDeploy's inability to trace multimodal-mandatory models; NVIDIA's own benchmark paper uses vLLM. How to recognize when to stop digging.
**Sources:** Decision Log §3.1–3.8, §4; backlog S-9.
**Targets:** linkedin, reddit:r/LocalLLaMA, hn

## [published 2026-07-02] what-i-designed-in-may-vs-what-shipped-in-june
**Angle:** The AI-generated architecture image (gorgeous, wrong in 7+ places within a month) vs the versioned Mermaid diagram. Plans are hypotheses; diagrams that can't be diffed will drift — and so do spec status lines (the audio spec said "Planned" for 3 weeks after the system was built).
**Sources:** the v1.6.4 image; Decision Log §v1.6-1, §v1.7-1; backlog M-6, M-7.
**Targets:** linkedin, hn

## [queued] sovereignty-as-routing-not-policy
**Angle:** Three LLM fallback chains, three guarantees — including the client profile that refuses to fall back, because silent degradation is worse than failure.
**Sources:** sovereign doc §Provider Fallback Routing + §Sovereignty Tiers; backlog S-11.
**Targets:** linkedin, reddit:r/selfhosted

## [queued] i-built-a-3-stack-ai-system-without-writing-code
**Angle:** The method post: discuss (multi-LLM) → consolidate → spec → execute (LLM-directed) → verify (gates) → operate. B.E. in CS, reads code, directs rather than writes. The repos are the evidence.
**Sources:** profile README; every repo's "How this was built"; resume.
**Targets:** linkedin, hn

## [queued] the-sam3-mask-that-crashed-vace
**Angle:** A days-long tensor-dimension bug that looked like a model problem and was a wiring problem: image-mode [1,H,W] masks vs video-mode [N,H,W].
**Sources:** creative repo problems-and-solutions glossary §8 + changelog; backlog C-7.
**Targets:** reddit:r/StableDiffusion, linkedin

## [queued] 25-ways-the-audio-stack-install-deviated-from-its-spec
**Angle:** Entry points lie, pin everything, the spec is a hypothesis — a tour of the LESSONS.md genre and why publishing failures beats hiding them.
**Sources:** audio repo LESSONS.md (all); backlog A-1..A-6.
**Targets:** linkedin, reddit:r/LocalLLaMA

## [queued] shipping-a-domain-site-and-offsite-backup-in-one-evening
**Angle:** The small lessons nobody writes down: parking DNS records block custom domains, zone-scoped API tokens can't touch Pages/Workers, storage boxes ship with all access toggles off, restic sftp needs relative paths, and Cloudflare Pages is quietly becoming Workers.
**Sources:** EXECUTION-PLAN 2026-06-11; SITE-DECISIONS.md §8; backlog I-5..I-9.
**Targets:** linkedin, reddit:r/selfhosted

## [queued] killing-the-two-pass-dance
**Angle:** Fitting a 30B multimodal LLM and a TTS engine on one 32GB RTX 5090: six boot attempts, why `gpu-memory-utilization` is a fraction of TOTAL validated against FREE, weights-in-VRAM ≠ checkpoint-on-disk (21.5GiB vs "~18"), and `--max-num-batched-tokens` as the hidden knob nobody mentions (16384→4096 freed the GiBs that utilization tweaking couldn't). Full draft already exists in the repo — bot adapts, not writes from scratch.
**Sources:** comic-manga-narrator docs/BLOG-killing-the-two-pass-dance.md; DEVLOG session 2; backlog P-8.
**Targets:** linkedin, reddit:r/LocalLLaMA, hn

## [queued] the-model-echoed-my-prompt-back
**Angle:** A real manga page lost ALL its dialogue because Nemotron returned `"dialogues[]"` — the prompt's array notation — as literal JSON keys, and `.get("dialogues")` silently got nothing. Model output variance as a distribution: same prompt at temp 0.1 produced pixel bboxes, normalized floats, plain keys, bracket keys, and occasionally no JSON at all (json_repair returns a bare string; callers .get() and die). The discipline: normalize at the parse boundary, validate types, retry once per item.
**Sources:** comic-manga-narrator docs/DEVLOG.md sessions 2+5 (bugs 3, 6, 9); docs/LESSONS.md §1-2; backlog P-6, P-7.
**Targets:** linkedin, reddit:r/LocalLLaMA, hn
## [published 2026-06-12 → object-removal-arc] i-benchmarked-open-source-object-removal-for-client-work
**Angle:** E1 bench, honestly scored: VOID removed a person from real footage in 149s on an RTX 5090 (27GB peak) — mid-clip fill statistically as stable as untouched pixels (frame-deltas 7.0-8.0 vs 8.3 control), BUT a partial-removal ghost where the subject crouched, half-resolution output, and a truncated tail. The verdict: not client-deliverable yet, and exactly what to fix next. Numbers, frames, and the scoring method included.
**Sources:** creative repo benchmarks.csv Vanisher row (2026-06-12); EXECUTION-PLAN E1 entry; /tmp/e1-frames methodology (frame-delta vs control region).
**Targets:** linkedin, reddit:r/StableDiffusion, hn

## [queued] the-bug-was-a-zero-size-stack
**Angle:** A Flutter game rendered pure blank — and three sophisticated theories (dual-GPU passthrough, the Flame engine, "X11 screenshots of GL animations lie") were all wrong. The fix was one line: a Stack sizes itself to its largest NON-positioned child, and a conditional `SizedBox.shrink()` overlay collapsed the whole game to 0×0 during play. What found it: replacing the subtree with a red ColoredBox and bisecting — plus a human eyewitness report that falsified the lying-screenshots theory. Debug by bisection, not theory; and when your verification tool might be lying, verify the verifier.
**Sources:** komorebi repo CHANGELOG 1.1.1 + commit 3ed3b9d (regression test); session memory 2026-06-12.
**Targets:** linkedin, reddit:r/FlutterDev, hn

## [queued] i-slept-while-the-llm-shipped-five-releases
**Angle:** "Finish all the phases, I'm going to sleep." Overnight: a pomodoro module, a physics tower game, v1.0 polish with whole-DB export, and an online leaderboard — each phase tested, committed, CI-green before the next began. The honest parts: the safety classifier refused to let the agent install services on production infra unattended (correctly), so the deploy became a script-as-deliverable blessed in the morning; one "lesson" recorded overnight turned out to be wrong and was corrected by daylight. Method post: phase gates, CI as the night watchman, and why autonomy needs refusal points.
**Sources:** komorebi repo commits e3f5998→19ebcd1 + CHANGELOG 0.7.0–1.1.0; server/arena/deploy.sh; session memory.
**Targets:** linkedin, hn

## [queued] multiplayer-without-servers-seed-the-rng-with-the-date
**Angle:** Real-time multiplayer needs WebSockets, state sync, and anti-cheat. A daily duel needs none of it: seed the piece RNG with the UTC date and everyone on Earth stacks the same blocks that day — a leaderboard row is the only network traffic. Fair, async, offline-tolerant competition from one integer. When "play together" actually means "compare honestly," determinism is the whole server.
**Sources:** komorebi lib/services/arena_api.dart (dailyMode/dailySeed) + seed-determinism test; server/arena/README.md modes section.
**Targets:** linkedin, reddit:r/gamedev, hn

## [queued] i-benchmarked-an-ai-avatar-pipeline-end-to-end
**Angle:** E2: photo + script → cloned-voice talking-head video, fully local, in ~5 min (TTS 25s + LatentSync 280s for 34.5s of speech). Gross sync verifiably correct (mouth tracks speech/silence against ffmpeg silencedetect), but lip-interior artifacts kill broadcast close-ups while staying fine for social-format. The honest deliverability line: avatar work is sellable at small format today, not at full-frame.
**Sources:** audio repo benchmarks.md E2 result; outputs/audio/lip-sync/5d4c4fca_lipsync.mp4.
**Targets:** linkedin, reddit:r/LocalLLaMA

## [published 2026-06-12 → object-removal-arc] same-pipeline-clean-vs-ghost-the-clip-makes-the-call
**Angle:** E1d closes the object-removal arc honestly. The exact same VOID+SAM3-video pipeline that left a ghost on a worst-case clip (subject behind glassware, low light) produces a CLEAN, deliverable removal on a representative clip (well-lit kitchen, separable spoon) — 180s, no ghost, perfect background reconstruction. The lesson for anyone evaluating AI tools: a single failure frame proves nothing; test selection IS the evaluation. Side-by-side frames + the dependency rabbit-hole (comfy-env silently disabling the video nodes) included.
**Sources:** creative benchmarks.csv Vanisher E1-E1d rows; COVERAGE.md; /tmp/e1d-frames.
**Targets:** linkedin, reddit:r/StableDiffusion, hn

## [queued] six-footguns-installing-a-local-ai-agent-on-ubuntu-24
**Angle:** Phase 2.5 of my Hermes install surfaced six environmental traps the spec never anticipated, each costing real time: (1) PEP 668 puts the `hermes` binary in `~/.local/bin` not `/usr/local/bin`, breaking every systemd `ExecStart`; (2) `pip install hermes[web]` doesn't actually install `aiohttp`; (3) the dashboard refuses `0.0.0.0` without `--insecure`; (4) nvm-managed Node is invisible to systemd (use NodeSource apt); (5) Workspace silently requires `HERMES_PASSWORD` when `HOST=0.0.0.0`; (6) the worst one — a profile-level `.env` with `override=True` silently REPLACED a strong random `API_SERVER_KEY` with a weak guessable value. Lesson: parameterize binary paths at install time, never trust extras-specs, and document config precedence aggressively.
**Sources:** sovereign doc Decision Log §v1.6.2-1 (the six + the four lessons); backlog H-12..H-17.
**Targets:** linkedin, reddit:r/selfhosted, hn

## [queued] my-tested-workflows-broke-from-upstream-drift
**Angle:** I ran a benchmark sweep across 9 ComfyUI workflows I'd built and "tested" weeks earlier. Result: only 5 of 9 still validate on the current ComfyUI — 4 broke from upstream drift. `ModelSamplingFlux.patch() got an unexpected keyword argument` (a node API changed under me), `Node 'Video Latent' has no class_type` (a custom node renamed/removed), Hunyuan workflows missing 4-5 nodes each. The honest lesson: "tested and working" has a SHELF LIFE on a fast-moving stack. A workflow is a snapshot against an environment that keeps moving; without pinned custom-node versions, your tested artifacts rot. Either pin everything, or treat re-validation as routine maintenance — and BENCHMARK PERIODICALLY so you discover the rot before a client does.
**Sources:** creative benchmarks COVERAGE.md sweep 2026-06-13; /tmp/sweep-results.csv; the 4 specific errors.
**Targets:** linkedin, reddit:r/comfyui, reddit:r/StableDiffusion, hn

## [queued] one-rtx-5090-how-many-users-the-honest-answer
**Angle:** "How many people can use a local LLM at once?" I stress-tested it instead of guessing. Sustained single-stream: 276 tok/s. Concurrency: 2 users=1.4x, 4=2.4x, KNEES at 8 concurrent (728 tok/s aggregate, flat after). vLLM continuous batching means throughput does NOT divide by user count, but KV-cache memory caps concurrent active generations at ~8 on 32GB. Translation: ~10-15 heavy or ~30-40 light chat users on one RTX 5090, latency under 2.5s.
**Sources:** sovereign nemotron-stress.csv; stress test 2026-06-13.
**Targets:** linkedin, reddit:r/LocalLLaMA, hn

## [queued] i-built-an-avatar-that-introduces-itself-as-an-avatar
**Angle:** E2: photo + script -> cloned-voice talking head, fully local, ~5 min. Honest quality with frames: gross lip-sync correct (mouth tracks speech vs silence, verified against ffmpeg silencedetect), but lip-interior artifacts at full-frame zoom = social-grade not broadcast close-up. The script is meta: the avatar says it's an avatar made on one machine.
**Sources:** audio benchmarks.md E2; public/avatar-lipsync-frames.png.
**Targets:** linkedin, reddit:r/LocalLLaMA

## [queued] a-self-healing-catalogue-for-a-stack-that-drifts
**Angle:** I benchmarked 9 ComfyUI workflows and found 4 had silently broken from upstream node renames (HunyuanVideoModelLoader -> HunyuanVideo15*, EmptyWanLatentVideo -> Wan22ImageToVideoLatent). "Tested" rots. So I built a monthly maintenance job: it validates every workflow against the LIVE node registry, auto-discovers new ones, regenerates a public catalogue with real numbers, and pings my phone with what drifted — but deliberately does NOT auto-"fix" renames (different I/O = a human/LLM rebuild call, not a safe auto-patch). The honest engineering line: automate detection and reporting fully; gate the risky repairs behind judgment.
**Sources:** scripts/workflow-validate.py + generate-catalogue.py + workflow-maintenance.sh; COVERAGE.md; benchmarks.csv.
**Targets:** linkedin, reddit:r/comfyui, hn

## [queued] my-uncensored-llm-was-leaking-raw-tokenizer-output
**Angle:** Benchmarking my two unrestricted local LLMs (the creative-pipeline script writers): CPU Nemotron PRISM ran clean at 18 tok/s, no refusals. But GPU Dolphin 24B AWQ — same uncensored role — came back at ~2 tok/s with `Ġ` and `Ċ` BPE artifacts leaking into the output text. Classic symptom of a chat-template/decode misconfig at the serving layer (awq_marlin @ 0.25 GPU-mem). Lesson: "it responds" isn't "it works" — inspect the actual bytes, and benchmark every model you rely on, because a misconfigured serving config produces plausible-looking garbage.
**Sources:** COSTING.md encoder section (private numbers); /tmp/encoder-bench.py; 2026-06-14.
**Targets:** linkedin, reddit:r/LocalLLaMA
## [manual] why-theinvalid-dot-me
**Angle:** The name story — reclaiming the worst insult. Personal; the human writes this one.
**Targets:** linkedin

---

## [queued] i-built-an-ai-agent-company-on-my-own-hardware
**Angle:** Stood up a Paperclip "company" (CEO/Builder/Reviewer) on a fully local Hermes+LiteLLM stack. It works — agents autonomously wrote code, tests, and a CI pipeline. But three honest lessons: (1) ~40s per-run overhead forces a two-tier design (fast Hermes-direct vs governed Paperclip); (2) the cheap CEO self-reviewed and APPROVED genuinely buggy code (an auth-less health check that reports a live service as DOWN) — mocked tests hid it; (3) model choice dominates everything (DeepSeek-Flash 0.8s vs Kimi 27-40s). Takeaway: a strong independent reviewer + a permanent human merge gate is load-bearing, not optional.
**Sources:** EXECUTION-PLAN.md 2026-06-14 entries; queue §B PA-1..PA-9; /data/ai/08-portfolio/devkit.
**Targets:** linkedin, reddit:r/LocalLLaMA, hn

# §B — Lesson & decision backlog (comprehensive, all projects, forever)

Priority: **H** = strong standalone post · **M** = good section/short post ·
**L** = footnote material, fold into related posts.
Status: `unmined` → `queued` (promoted to §A) → `published` (date).

## Sovereign stack (S)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| S-1 | H | iGPU/headless decision: monitor off the 5090 → ~1GB TOTAL VRAM reclaimed (NOT "~25K ctx" — see S-18 correction) | Decision Log §13 | published (32gb-budget) |
| S-2 | H | Sequential modes: never co-load — purge VRAM, fresh-load per task | §12 | queued (32gb-budget) |
| S-3 | H | NVFP4 over FP8/BF16: the only quant leaving real KV headroom on 32GB | §weights rationale | queued (32gb-budget) |
| S-4 | M | 180K not 256K: leave 25% context headroom, bump only on real errors | §7 | queued (32gb-budget) |
| S-5 | M | FP8 KV cache ≈ free 2× context for description tasks — CORRECTED: tiny effect on a hybrid, KV isn't the constraint (see S-18) | §8 | superseded → S-18 |
| S-6 | M | EVS off for forensic work: 30% slower beats missed details | §9 | unmined |
| S-7 | M | Tiered triage (hero/secondary/category/atmospheric) stops hallucinated enumeration in dense scenes | §10 | unmined |
| S-8 | M | Capture model reasoning traces as provenance for client work | §11 | unmined |
| S-9 | H | TRT-LLM: 8 distinct failures (schema, subcommand, mamba_ssm, CUTLASS, use_cache, HF_HUB_OFFLINE, deps, pixel_values) | §3.1–3.8 | queued (trt-llm post) |
| S-10 | M | KV-cache ceiling: 3–5 concurrent agents max; "60-agent local swarm" claims = serialized or cloud fan-out | v1.4 analysis | unmined |
| S-11 | H | Three profiles, three guarantees; client profile refuses fallback | §Routing | queued (sovereignty post) |
| S-12 | M | Always-on CPU floor (llama.cpp, 60GB RAM): agents survive GPU purges | Phase 4.5 | unmined |
| S-13 | L | llama.cpp builds <9283 lack nemotron_h_moe arch — rebuild after pulls | memory/lessons | unmined |
| S-14 | M | Tool-evaluation checklist; "LLM recommends with zero trade-offs" = red flag | §6, v1.4 | unmined |
| S-15 | L | Keep failed-attempt artifacts on disk deliberately (TRT-LLM dirs) — they're the receipts | §15 | unmined |

## Hermes / LiteLLM / cockpit (H)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| H-1 | M | `fallback_providers` is top-level in profile YAML — nested form silently ignored | memory/litellm-lessons | unmined |
| H-2 | M | Profile-scoped .env pre-empts global .env (HERMES_HOME set before dotenv) | same | unmined |
| H-3 | L | Provider slug `custom` not `openai_compatible` (v0.14 canonical list) | same | unmined |
| H-4 | M | LiteLLM image ignores mounted config without explicit `--config` flag → database-only mode, cryptic 400s | same | unmined |
| H-5 | L | LiteLLM needs both docker networks (postgres bridge + phoenix) for OTEL | same | unmined |
| H-6 | L | Phoenix OTLP = gRPC :4317, not HTTP :4318 | same | unmined |
| H-7 | M | COOKIE_SECURE=0 required for PWA over HTTP-on-tailscale (cookie RFC) | §v1.6.3-1 | unmined |
| H-8 | M | Pin HERMES_DASHBOARD_SESSION_TOKEN or every restart breaks remote pairing | §v1.7-1 | unmined |
| H-9 | L | Port collisions: verify actual ports (DeerFlow :2026 not :3000) before reserving | §v1.6.1-1, §v1.6.4-5 | unmined |
| H-10 | M | Cockpit churn: Aion UI → 3rd-party PWA → official Desktop in 6 weeks — UI layers are the least stable part of the stack | §v1.6-1, §v1.7-1 | unmined |
| H-11 | M | Free-tier cloud cascade (Option B): budget caps + cooldowns make $0 inference reliable | §v1.6.4-2 | unmined |
| H-12 | H | "0.0.0.0 ≠ exposed": a port scan showing `0.0.0.0` isn't a vuln if the firewall layer gates it — LiteLLM :4000 (Docker) is LAN-dropped by DOCKER-USER; Hermes :8642 (host proc) is UFW tailnet-only. Rebinding either would FIGHT the design / break localhost clients | 2026-06-29 infra pass | unmined |
| H-13 | M | App defaults squat your always-on floors: DeerFlow's gateway host-path defaulted to :8001 = collides with Nemotron CPU PRISM (sovereignty floor); its `make dev` could never bind. Moved to :2027. Always `ss -tlnp` before trusting a vendored default (extends H-9) | 2026-06-29 infra pass | unmined |
| H-14 | H | A floating `:main-latest` tag is a time bomb: LiteLLM silently drifted to 1.82.6, whose anthropic /v1/messages pass-through now defaults OpenAI backends to the Responses API → vLLM 400 "input should be a valid string". Claude-Code-via-LiteLLM (codelocal) needs `use_chat_completions_url_for_anthropic_messages: true`; fix the deeper cause by pinning the image by DIGEST | 2026-06-30 codelocal fix | unmined |

## Creative stack (C)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| C-1 | H | Wan 2.2 "crystalline shadow": days-long artifact bug = missing two-sampler MoE chain (wiring, not model) | glossary §1 | unmined |
| C-2 | M | FLUX 4B base vs distilled filename trap | glossary §2 | unmined |
| C-3 | H | `docker system prune` deleted the ComfyUI image — prune is not housekeeping when containers are stopped | glossary §3 | unmined |
| C-4 | M | Migrating docker data-root under three live stacks without breaking them | glossary §4 | unmined |
| C-5 | M | Multi-stack boundary discipline: don't nuke the neighbors (shared daemon, shared GPU) | glossary §5 | unmined |
| C-6 | M | Local patches (4× WanVideoWrapper VRAM) must be documented + re-applied after every update | glossary §6 | unmined |
| C-7 | H | SAM3 image-mode vs video-mode masks crashed VACE (tensor dims) | glossary §8 + changelog | queued (sam3 post) |
| C-8 | L | VOID subgraph "Prompt has no outputs" trap | glossary §7 | unmined |
| C-9 | L | VACE color splotches = unconnected mask input | glossary §8 | unmined |
| C-10 | L | flash_attn KeyError was a false alarm — read the actual traceback origin | glossary §9 | unmined |
| C-11 | M | Tool churn (SeedVR2 v2.5, RTX VSR re-eval): re-verify "facts" monthly in fast ecosystems | glossary §10 | unmined |
| C-12 | H | The reframe: client work is video EDITING not generation — changed the whole architecture tier | glossary §11, v1.3 | unmined |
| C-13 | M | An upstream model repo vanished overnight — mirror critical dependencies, check licenses early | v1.5 changelog | unmined |
| C-14 | M | VOID removed the object AND its reflection — capability discoveries belong in test logs | v1.4 notes | unmined |
| C-15 | M | Forensic bridge: dense scene constraints starve diffusion of room to hallucinate | v1.5 bridge files | unmined |
| C-16 | M | Specs that carry live status go stale silently: the creative doc kept drawing "Hermes Agent (cloud)" for ~2 months after Hermes moved to a LOCAL systemd service (:8642), and the audio doc lagged its real phase state. Fix = split the axes: specs hold *design*, a single STACK-STATUS.md holds *truth*, updated every change. C1 reconciliation 2026-06-22: creative doc → v1.5.2 (diagram + "how Hermes controls your machine" rewritten local/no-SSH), audio doc → v1.1. | creative-stack-v1.5.2; STACK-STATUS.md; EXECUTION-PLAN C1/C2 | unmined |

## Audio stack (A) — details in repo LESSONS.md
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| A-1 | H | Entry points lie: 3 of 5 model repos had different real entry points than their README | LESSONS.md | queued (25-ways post) |
| A-2 | M | faster-whisper needs CTranslate2 conversion + preprocessor_config (n_mels silent corruption) | LESSONS.md | queued (25-ways) |
| A-3 | M | Fish Speech pinned to v1.5.1: code and weights must move together | LESSONS.md | queued (25-ways) |
| A-4 | M | Hallo2: six stacked fixes (xformers index, NCCL, GLES symlink, eager attn, output path, right repo org) | LESSONS.md | queued (25-ways) |
| A-5 | M | YuE: LFS pointer stubs from plain git clone; sdpa patch; mono MP3 output | LESSONS.md | queued (25-ways) |
| A-6 | M | RQ pattern: gateway enqueues, separate GPU worker consumes — and the worker needs the full nvidia runtime block | LESSONS.md | queued (25-ways) |
| A-7 | M | Unified workflow catalogue: one page now merges scriptwriting (Nemotron/Dolphin) + creative + audio; each tile carries BOTH a quality tier and a capability, with a client-side Group-by + filter toggle (default = tier). Driven by audio-benchmarks.csv with honest measured/estimate/perf-pending/blocked badges — only 3 audio pipelines have hard numbers, empty cells stay empty | generate-catalogue.py rewrite; audio-benchmarks.csv | unmined |
| A-8 | L | Status-from-notes detection must match an explicit uppercase `BLOCKED` marker, not a lowercased "block" substring — Vanisher's prose ("dep that blocked the video nodes") false-flagged the tile as blocked until the check was tightened | generate-catalogue.py status_of() | unmined |
| A-9 | H | The audio gateway's music worker pointed ACE-Step at a non-existent inference.py (that path holds the WEIGHTS, not the code); Stable Audio needs `stable_audio_tools` which requires Python<3.11 (worker is 3.12). Fix: ACE-Step runs in an ISOLATED venv (`--system-site-packages` reuses the SM_120 torch, deps isolated so YuE/Fish Speech untouched) → real **stereo 48kHz** instrumental in ~10s (1.8s diffusion). Also: Maestro's "ACE-Step" score was actually YuE 7B all along | ace-step-repo + ace-step-venv; workers/music.py | unmined |
| A-10 | H | ALL 3 local lip-sync models broken in the consolidated worker: LatentSync emits structurally corrupt output (mouth melt/seams; silent diffusers drift), Hallo2 hits a diffusers API break (`_set_gradient_checkpointing 'enable'`) + missing libGLESv2, MuseTalk blocked on mmcv/cu130. Root cause: one shared env can't satisfy models that need pinned isolated envs (they were separate containers originally). Local lip-sync = social-grade ceiling; broadcast = cloud. Rebuild target = MuseTalk 1.5 | musetalk-1.5-rebuild-spec.md | RESOLVED → A-11 (MuseTalk) / A-13 (Hallo2) / A-14 (LatentSync 1.6) — all 3 now rebuilt as dedicated pinned services |
| A-11 | H | MuseTalk 1.5 REBUILT & working (2026-06-21). The mmcv/SM_120 wall is unwinnable on RTX 5090: no `mmcv._ext` wheel for torch 2.8/cu128, and `mmcv-lite` can't satisfy mmpose (it imports EDPoseHead→`mmcv.ops` at import time). Fix: drop mmpose entirely and run the SAME `dw-ll_ucoco_384` DWPose model via **rtmlib + onnxruntime** — identical 133-keypoint output, zero mmcv. Packaged as a dedicated isolated image (`FROM mushishi-audio-base` + rtmlib) behind `creative-musetalk:9005`; the worker's `lipsync.py` museTalk branch HTTP-calls it like it calls creative-tts, so the YuE+Fish worker is never touched. Three more traps fixed: torch 2.6+ `weights_only=True` default rejects MuseTalk's legacy face-parse checkpoints; image inputs hit a `save_dir_full` NameError in inference.py; whisper-**tiny** (hidden=384, matches the PE) is mandatory — whisper-large is the wrong dim. Verified e2e through the gateway: 78s for a 34.5s 1024×1024 talking-head, ~7.7GB peak, VRAM freed on idle, social-grade (coherent mouth, no melt/smear). | musetalk-1.5-rebuild-spec.md; 06-configs/audio/Dockerfile.musetalk; museTalk/musetalk_server.py; preprocessing.py | unmined |
| A-12 | H | Cross-language dub = a hard 32GB VRAM wall, and a rebuild that bit itself (2026-06-22). Baking libgles2/torchsde into the worker image pulled unpinned `onnxruntime-gpu` to 1.27.0 (needs CUDA 13) on a CUDA-12.8 base → `libcudart.so.13` ImportError broke transcribe/dub; the ACE-Step smoke test missed it (no onnxruntime path). Pinned ==1.26.0. Then dub Option B (load GPU Nemotron per job, purge after): translation ROUTING works (dub.py→LiteLLM `sovereign-only`→:8000; `cooldown_time:0` so a per-job model isn't locked out; worker reaches the firewalled Nemotron only via LiteLLM), but the pipeline needs the ~26GB Nemotron + Whisper + Fish Speech AT ONCE (~31.7GB) — no co-load fits. SOLVED via a phased cross-dub.sh: one model at a time (transcribe→restart worker to free Whisper→load Nemotron→translate→purge→TTS→mux, ~29GB peak), validated EN→ES. Bonus: the GPU Nemotron is a reasoning model — needs detailed-thinking-off + big max_tokens or empty content. | gateway/workers/dub.py; scripts/cross-dub.sh; litellm config sovereign-only | unmined |
| A-13 | H | Hallo2 cinematic lip-sync rebuilt (2026-06-22), restoring the tier MuseTalk can't do (head pose + expression + CodeFormer) as its own pinned `mushishi-hallo2` image behind `creative-hallo2:9006`. Two lessons. (1) **The smallest correct pin beats the model's own lockfile.** The only real break is diffusers — Hallo2 ships its own UNet overriding `_set_gradient_checkpointing(module, value)` (old sig), so base diffusers 0.38 (`enable=`) raised `unexpected keyword 'enable'`; fix = pin `diffusers==0.32.2` ALONE. Porting Hallo2's whole requirements trio onto a newer base is actively wrong: `numpy==1.26.4` breaks the base's numpy-2-native scipy 1.18 (`np.long`, re-added in numpy 2.0) → insightface crash. (2) **A version trap can hide at the LAST step:** base `decorator 5.3.1` breaks moviepy 1.0.3's mp4 mux (`fps=None`) → pin `decorator==4.4.2`; the tell was every COMPUTE step passing and only file-WRITING failing → suspect an I/O lib, not the model. Cinematic is expensive: ~13.3GB, ~35× slower than realtime (1197s for a 34.5s 512² clip) vs MuseTalk's 78s — ~15× slower. Verified e2e (lipsync quality=cinematic→hallo2). | hallo2-rebuild-spec.md; 06-configs/audio/Dockerfile.hallo2; hallo2/hallo2_server.py; gateway/workers/lipsync.py | unmined |
| A-15 | M | **ComfyUI holds its models in VRAM after a generation — free them before any co-resident GPU job** (2026-06-22, lived while making the Hallo2 half-body sample). Generated a FLUX.1-dev half-body portrait via ComfyUI, then immediately ran a Hallo2 cinematic job through the gateway → `torch.OutOfMemoryError: ... 16.94 MiB is free`. Cause: ComfyUI cached the ~20GB FLUX model resident; Hallo2 (~13GB) had nowhere to load. Both fit the 32GB card *individually* — it's the co-residence that doesn't, the exact VRAM-sequencing lesson from the dub work (A-12). Fix: `POST http://localhost:8188/free {"unload_models":true,"free_memory":true}` to drop ComfyUI back to ~3GB baseline, then the job runs. Lesson: a model server that loads-on-demand does NOT necessarily free-on-idle the instant the *request* ends — verify `nvidia-smi` is actually back to baseline before launching the next stack's GPU job, don't assume. (Bonus: Hallo2 outputs 512² but PRESERVES a half-body input's framing — head+torso+gesture survive, so the cinematic tier's non-headshot differentiator shows even at 512².) | ComfyUI /free endpoint; gateway lipsync cinematic; benchmarks/samples/avatar-hallo2-halfbody.mp4 | unmined |
| A-14 | H | LatentSync 1.6 rebuilt as the **production** portrait tier (2026-06-22), closing the A-10 corruption. Three lessons. (1) **The same one-line pin fixed a THIRD model** — `diffusers==0.32.2` ALONE. LatentSync 1.5's "melt + affine seams" was never a tuning/margin bug; it was silent diffusers drift (base ≥0.32.0 → ~0.38) in the shared worker, the identical root cause as Hallo2 (A-13). Dedicated pinned image (`FROM mushishi-audio-base` + that one pin) behind `creative-latentsync:9007`; no decorator pin (LatentSync muxes via imageio not moviepy), no transformers pin (it uses its OWN bundled whisper). (2) **A "1.6" can be a pure checkpoint swap** — the ByteDance card says 1.6 is the SAME code/strategy as 1.5, only the 512² training set + checkpoint differ ("load the checkpoint, set resolution"). So the rebuild was: download the 1.6 `latentsync_unet.pt`, run on the existing `stage2_512.yaml` (already res=512), reuse 1.5's whisper-tiny + insightface buffalo_l (byte-identical) via the repo `checkpoints` symlink, pre-warm sd-vae-ft-mse into the shared HF cache. Result: **regression fully closed on BOTH portraits** that broke 1.5. (3) **Honest A/B can end in "kept anyway, not because it won."** The spec made this the one rebuild allowed to end in *drop it* — it had to be *visibly sharper than MuseTalk* on teeth/lips to earn its keep. It WASN'T: teeth/lip sharpness ≈ a tie, and it costs ~3× wall-clock (242s/34.5s) + ~2.6× VRAM (~20GB, not the doc's stale ~6GB est — 512² latent diffusion + VAE + DeepCache is heavy). Its real edge is cleaner lower-face/beard blending (no MuseTalk paste-box) + more natural skin; user kept it as the production quality tier for that, draft stays MuseTalk. The benchmark records the tie honestly. | latentsync-1.6-rebuild-spec.md; 06-configs/audio/Dockerfile.latentsync; latentsync/latentsync_server.py; gateway/workers/lipsync.py | unmined |

## Infra / ops (I)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| I-1 | H | cron PATH ≠ shell PATH; monitor outcomes not exit codes; heartbeat the watchdog | post live | published 2026-06-10 |
| I-2 | H | Docker bypasses UFW (DOCKER-USER chain fix; verify from untrusted device) | LESSONS.md | queued |
| I-3 | H | Docker data-root move ≠ containerd store move (169GB on /var) | EXECUTION-PLAN | queued |
| I-4 | H | Nightly wheels depreciate; runtime pip installs rot on container recreation | audio LESSONS | queued |
| I-5 | M | Registrar parking DNS records block Cloudflare custom domains | 2026-06-11 | queued (one-evening) |
| I-6 | M | Zone-scoped CF tokens can't manage Workers/Pages (account-level perm) | same | queued (one-evening) |
| I-7 | M | Hetzner Storage Boxes ship with ALL access toggles off — looks like wrong password | same | queued (one-evening) |
| I-8 | L | restic sftp needs relative path on storage boxes (`:./repo` not `:/repo`) | same | queued (one-evening) |
| I-9 | L | Cloudflare Pages → Workers migration: new sites need wrangler.jsonc assets config | same | queued (one-evening) |
| I-10 | L | smartd on Ubuntu: enable error is an alias quirk; real unit is smartmontools.service | 2026-06-10 | unmined |
| I-11 | L | GitHub email-privacy rejects pushes with real email — use the noreply identity from commit #1 | same | unmined |
| I-12 | M | Backup coverage inventory question: "can I re-download this?" — everything answering no goes in | backup.sh history | unmined |
| I-13 | M | Docs referenced a script (agent-mode.sh) that never existed — phantom-reference drift | 2026-06-10 | unmined |
| I-14 | M | GUI mode-picker at login: zenity + autostart beats remembering mode scripts | mode-picker.sh | unmined |
| I-15 | H | Headless `claude -p` in cron died silently: OAuth access token expired with an EMPTY refresh token (can't self-renew) AND the CLI prints the 401 but EXITS 0 — so the generic "did not publish" alert hid the cause for days. Fix = long-lived `claude setup-token` in a dedicated env file (decouples cron from the browser-refresh OAuth) + a preflight auth probe + a content-based (not exit-code) auth-fail alert + a proactive credential-expiry check in the watchdog. The I-1 lesson ("monitor outcomes, not exit codes") recurring in a new disguise. Two follow-on traps when restoring: (a) a wrapped paste split the token across two lines → only a truncated prefix assigned → 401 + a bare-token "command not found" when sourced (tokens must be ONE line, no whitespace); (b) the CLI's persisted GLOBAL default model had drifted to the local LiteLLM alias `coding`, which a headless cloud run can't use → pin `--model` in the script, never trust the ambient default. | 2026-06-24 blog-bot incident; blog-bot.sh/healthcheck.sh | unmined |
| I-17 | H | **A safety gate that conflicts with a mandatory convention doesn't fail loud — it deadlocks FOREVER.** blog-bot refused to publish unless the tree was clean (its prompt ran `git add -A`, and push = live deploy), but the repo's own rule makes every session dirty pipeline/queue.md with lesson rows while pushing stays owner-gated — so the tree is dirty most of the time BY DESIGN, and the gate silently froze 21 queued posts for 5 days (blog incident #3, after OAuth-expiry and the catalogue-mirror dirtying). Gate and convention were each individually correct and jointly a deadlock. Fix = remove the gate's REASON, not the convention: publish stages an explicit pathspec (the post + its public/ assets + queue.md, whose rows ride along — public by convention), unrelated dirt is left alone and reported in a non-blocking INFO, and a post-push allowlist check alerts loudly if a commit ever sweeps out-of-pathspec files. GIT-1 ("commit with explicit pathspec in any repo you didn't just clean") applied to the bot that violated it. | 2026-07-02 blog-bot incident #3; blog-bot.sh + weekly-draft.md step 7 | unmined |

## Products (P) — komorebi, comic narrator, arena
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| P-1 | M | ffmpeg zoompan requires even-aligned dimensions (2.5D parallax gotcha) | comic narrator session | superseded → P-12 |
| P-2 | M | Job-status contract between app and RQ gateway: design the status vocabulary first | comic narrator session | unmined |
| P-3 | M | PocketBase as a one-binary leaderboard backend: anon-create capped + admin-only mutations | komorebi arena deploy | unmined |
| P-4 | L | Fresh VPSes lack unzip — deploy scripts must self-install their tools | arena deploy.sh | unmined |
| P-5 | M | Local-first app + opt-in online features: the sync boundary as a privacy feature | komorebi SPEC | unmined |
| P-6 | H | Models echo prompt notation as literal output keys ("dialogues[]"); normalize at parse, never only at prompt | comic-narrator LESSONS §1, bug 6 | queued (model-echo post) |
| P-7 | M | Model variance is a distribution: coerce geometry, type-check parses (json_repair returns bare strings), retry once per item | LESSONS §2, bugs 3/9 | queued (model-echo post) |
| P-8 | H | vLLM coexistence on 32GB: util = TOTAL-fraction checked vs FREE; weights-in-VRAM ≠ disk; --max-num-batched-tokens is the hidden knob | LESSONS §7-9, BLOG draft | queued (two-pass post) |
| P-9 | H | The model already extracts what the pipeline ignores (tone/pacing/bboxes were dead data for 5 sessions) — immersion gaps are consumer problems | LESSONS §3, §12 | unmined |
| P-10 | M | Forensic soundscape: ambient from what's VISIBLE (cow → moo), per-panel beds, duck under speech — the author drew the sound sources on purpose | DEVLOG sessions 3+5, Track E | unmined |
| P-11 | M | ALL-CAPS lettering is typography, not speech: interjection lexicon for TTS, original lettering for subtitles | LESSONS §13 | unmined |
| P-12 | M | Own your camera math: one Python trajectory consumed by background AND overlay beats reverse-engineering zoompan (incl. its undocumented even-snap) | LESSONS §10, supersedes P-1 | unmined |
| P-13 | M | License-first voice sourcing: emotive corpora are NC; pitch-ladder autocorrelation selects speakers with zero metadata; cloning follows accent AND affect | LESSONS §14-15, VOICES.md | unmined |
| P-14 | M | Voice-actor cloning is a publicity-rights violation; similarity CASTING (acoustic distance over licensed banks) is the legal equivalent | VOICES.md, match-voice.py | unmined |
| P-15 | M | Cast diversity must be explicit: score ties collapsed an entire cast (narrator included) onto dict-order-first | bug 10 + en-parity fix | unmined |
| P-16 | M | Unit tests don't cover seams: a function reduced to return-None passed 24 green tests; the smoke test added after caught a real regression in hours | LESSONS §19-20, bugs 8 | unmined |
| P-17 | L | Common Voice moved to Mozilla Data Collective (Oct 2025): HF repos are stubs, no search API (enumerate sitemap.xml), per-dataset web terms gate, CSV field-limit overflow | DEVLOG session 6 | unmined |
| P-19 | H | "Run it together or break it down?" wasn't the question: a 1.4GB PDF was 4684 vertical webtoon strips, not 179 pages — inspect before deciding | comic-narrator session 9 | unmined |
| P-20 | M | Webtoons break paged pipelines 3 ways: 300-DPI ingestion OOMs (extract native), tall strips blind the vision encoder (slice by gutters), ~20.5k panels = ~14 GPU-days | DEVLOG session 9, Track F | unmined |
| P-21 | M | The right answer to an impossible batch is a slicer + a one-episode stress test, not a 2-week monolith — same lesson as the first real manga page | session 9 | unmined |
| P-22 | H | Per-clip AAC slices + -c copy concat = boundary clicks AND long dead-audio spans; mux ONE continuous narration over the assembled video instead | comic-narrator session 10 | unmined |
| P-23 | M | A camera move that "works in one scene and not the next" is the wrong primitive — a static frame + speaker spotlight halo beats zoom-roulette | session 10 review | unmined |
| P-24 | M | We built a forensic vision extractor then drove the render from crude bboxes; the unlock is asking for director signals (shot_type, action_intensity, reading_path) | TTS-RESEARCH.md | unmined |
| P-25 | L | SFX one-shots must be trimmed (~2.5s) or N action cues stretch a panel to ~19s of dwell | session 10 | unmined |
| P-26 | H | Character voice consistency = comic re-identification: emit an `appearance` per character + feed the running cast sheet back to the VLM so it REUSES labels (Manga Whisperer approach); fixed 23 labels → 8 identities, 0 inconsistent voices | comic-narrator session 11 | unmined |
| P-27 | M | Role-based casting: protagonist→fixed lead voice, leads→distinct pool, faceless crowd→shared voice; prominence hierarchy AND caps the cast explosion | session 11 | unmined |
| P-28 | H | The "double run" (expressive TTS → voice conversion) is the established technique to decouple ACTING from IDENTITY; Seed-VC zero-shot beats RVC for a reference-clip bank (no per-voice training) | TTS-RESEARCH.md | unmined |
| P-29 | H | Validate a risky install in an ISOLATED venv first: parler-tts pulls torch-stable which would downgrade the worker's nightly-cu130 and break the RTX-5090 stack — caught before touching the container | session 11 torch trap | unmined |
| P-18 | L | concat demuxer wants uniform streams (the establishing shot carries silent AAC for this); alpha intermediates need ProRes4444/VP9 — libx264 cannot | LESSONS §5, §11 | unmined |
| P-19 | H | Flutter Stack sizes to its largest NON-positioned child — a conditional SizedBox.shrink() collapsed a game view to 0×0; red-box bisection beat three days of GPU/engine theories | komorebi 3ed3b9d, CHANGELOG 1.1.1 | queued (zero-size-stack post) |
| P-20 | M | When the verification tool might lie, verify the verifier: blank X11 captures of a live GL window spawned a false lesson; a human eyewitness falsified it — keep eyewitnesses in the loop | komorebi session 2026-06-12 | queued (zero-size-stack post) |
| P-21 | M | drift stream `.first` inside mutations/dialogs deadlocks under widget-test fake-async (bit twice); reads in write paths use one-shot get() | komorebi CHANGELOG 0.3.0 + 0.6.0 fixes | unmined |
| P-22 | H | Date-seeded RNG = async multiplayer with zero realtime infra: same daily piece sequence for everyone, scores compare fairly | komorebi arena_api.dart dailySeed | queued (date-seed post) |
| P-23 | M | Overnight autonomous phases with CI gates; safety classifier correctly refused unattended prod-VPS installs → ship the deploy as an idempotent script to bless in the morning | komorebi overnight run 2026-06-12 | queued (slept-while-shipping post) |
| P-24 | M | GitHub macOS runners give a Linux-only shop continuous iOS/macOS compile verification (free on public repos); signing stays the only Mac-bound step | komorebi ci.yml apple job | unmined |
| P-25 | L | 800MB /tmp: flutter test/build scratch dirs fill it and runs die mid-suite or hang — export TMPDIR to the big disk | komorebi session | unmined |
| P-26 | L | System openjdk was JRE-only → Gradle "toolchain does not provide JAVA_COMPILER"; user-space Temurin JDK + flutter config --jdk-dir fixes without sudo | komorebi android build | unmined |
| P-27 | L | ListView(children:) in bottom sheets is viewport-lazy — scrollUntilVisible before asserting below-the-fold widgets in tests | komorebi widget tests | unmined |
| P-28 | L | Game loops without an engine: event-loop Timer + CustomPainter repaint Listenable beats setState-per-frame from a Ticker (and sidesteps embedder quirks) | komorebi tower_view.dart | unmined |

## Bench & headless-ComfyUI session (E, 2026-06-12)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| E-1 | H | Driving ComfyUI headless via its OWN frontend: TCP proxy + browser preview + `app.loadGraphData()`/`app.queuePrompt()` — sidesteps UI-vs-API JSON formats AND subgraph conversion | this session | unmined |
| E-2 | H | E1 bench results: 149s/27GB person removal, stable fill, t=0.5 ghost, half-res output | benchmarks.csv | queued (bench post) |
| E-3 | M | ComfyUI container doesn't mount input/ — UI-uploaded videos die with container recreation; mount it or docker cp | compose inspection | unmined |
| E-4 | M | creative-mode.sh pointed at a compose dir that never existed — phantom-path drift, found only when actually run | this session | unmined |
| E-5 | M | VOID outputs half-resolution + caps frames — editing tier needs the upscale pass chained for deliverables | E1 bench | unmined |
| E-6 | L | Chrome+extension refused a localhost port that curl could reach — when one client fails, try another before debugging the server | this session | unmined |
| E-7 | H | E1b: ghost root-caused — Vanisher uses image-mode SAM3_Detect (no temporal propagation) so masks drop when subject is occluded; same bug class as the Shapeshifter fix. Frame-cap and fill-prompt fixes verified (6s full duration) | E1b bench 2026-06-12 | unmined |
| E-8 | M | Custom-node updates shift widget order — saved UI workflows silently misalign values into wrong slots (resolution=0); construct API prompts from live /object_info instead | crystalforge debugging | unmined |
| E-9 | M | After restarting ComfyUI, the browser frontend keeps a stale node registry — reload the page or unknown-class nodes serialize without class_type | same | unmined |
| E-10 | M | Runtime-pip-rot case #3: SeedVR2 deps (rotary_embedding_torch, omegaconf) died with container recreation; dry-import loop (import → catch ModuleNotFoundError → pip install → repeat) finds the full set in one pass | same | unmined |
| E-11 | L | The fill prompt said 'empty sidewalk daylight' in a laboratory scene — template prompts left over from other tests quietly degrade results; always read the actual widget values | E1b | unmined |
| E-12 | H | E1c: SAM3 video pipeline grafted into VOID via direct API-graph surgery (4 video nodes replace SAM3_Detect, quadmask rewired). Root infra fix: comfy-env==0.3.89 was a missing pip dep silently disabling ALL SAM3 *video* nodes (image SAM3_Detect came from same pack but loaded). Ghost reduced not eliminated on worst-case clip — honest finding: mask-tracking fixed, residual is inpaint-quality limit | E1c bench 2026-06-12 | unmined |
| E-13 | M | Worst-case test selection inflates failure: subject behind transparent glassware in low light is near-impossible for any inpainter; bench client-readiness on REPRESENTATIVE footage, document the hard case separately | E1c | unmined |
| E-14 | M | Quantitative frame-diff metrics need a motion-free reference region — measuring a ghost box that overlaps a spinning fan reports fan motion as ghost energy (false null result); pick static background for deltas | E1c quant attempt | unmined |

## Coverage-audit additions (2026-06-13, gaps found vs stack docs)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| S-16 | H | FlashInfer MoE backend is broken on consumer Blackwell (SM_120) — Nemotron MoE crashes; `--moe-backend triton` is the fix. Exact-error SEO gold | Decision Log + sovereign README | unmined |
| C-16 | M | SeedVR2 is FP16-ONLY (NVFP4 port fails, VAE/DiT entangled), needs multi-step 720→1080→2160 not one jump, blocks_to_swap 32, and "never 30s+ per batch" is the health signal | creative catalogue v1.4 Crystalforge gotcha | unmined |
| C-17 | L | Distilled vs base run-settings are NOT interchangeable: distilled = 4 steps/CFG 1.0 (raising steps hurts), base = 20 steps/FluxGuidance 3.5 (4 steps = soft/under-formed). Same model family, opposite knobs | catalogue Flashfire/Goldsmith gotchas | unmined |
| S-17 | L | Hunyuan is NOT MoE — single sampler is correct (vs Wan 2.2 which REQUIRES two); applying the two-sampler fix to Hunyuan would be wrong | catalogue Hunyuan "DO-NOT-TOUCH" | unmined |

## Stress-test & multi-user (B-blocked, GPU)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| B-1 | H | Concurrency stress test (ramp simulated users, find tok/s + latency knee) — THE number needed to quote multi-employee deployments. Continuous batching means throughput does NOT divide by user count; KV-cache memory is the real limit (3-5 long-ctx sessions = wall) | COSTING.md multi-user block; user q 2026-06-13 | blocked-on-gpu |
| B-2 | M | Full Nemotron stress test at session end (sustained max-load tok/s, thermal, power, throttle) — proper numbers vs the passive 139-avg observation | user req 2026-06-13 | blocked-on-gpu |
| B-3 | M | Tier-5 PRIVATE capability benchmark (stats only, never public price/imagery) — see COSTING.md positioning | user 2026-06-13 | blocked-on-gpu |

## Coverage-audit additions (2026-06-13) — gaps found grepping stack docs vs backlog
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| H-12 | H | PEP 668: pip --break-system-packages lands binaries in ~/.local/bin not /usr/local/bin — parameterize systemd ExecStart via `which X` at install | §v1.6.2-1.1 | queued (six-footguns) |
| H-13 | M | `pip install foo[web]` extras can silently omit deps (aiohttp missing from hermes[web]) — verify imports, don't trust extras-spec | §v1.6.2-1.2 | queued (six-footguns) |
| H-14 | M | nvm-managed Node is invisible to systemd services — use NodeSource apt Node for system services, nvm for interactive dev | §v1.6.2-1.4 | queued (six-footguns) |
| H-15 | H | The silent `.env override=True` security bug: a profile-level .env replaced a strong random API key with a weak guessable one — precedence bugs are security bugs | §v1.6.2-1.6 | queued (six-footguns) |
| H-16 | L | Dashboard refuses 0.0.0.0 without --insecure; Workspace needs HERMES_PASSWORD at HOST=0.0.0.0 | §v1.6.2-1.3,5 | queued (six-footguns) |
| H-17 | M | Credential rotation: keys pasted into an LLM chat for handover were rotated to openssl rand -hex 32 — documented rotation procedure now exists | §v1.6.2-1 side effect | unmined |
| H-18 | M | Hermes Desktop remote-gateway: pin HERMES_DASHBOARD_SESSION_TOKEN (else fresh uncopyable token each start) + dashboard MUST run --tui or chat WebSocket silently fails (#1 failure mode) | §v1.7-1 two requirements | unmined |
| S-16 | L | Tailscale travel test: direct peer path held through lid-close on mobile hotspot, no DERP; CGNAT/corp-FW may force DERP — re-test if encountered | §v1.6.4-6 | unmined |
| S-17 | L | CPU Nemotron is the PRISM abliterated/unrestricted variant — deliberate: unrestricted local floor, never client-routed | §v1.6.4-1 | unmined |

**Audit verdict:** sovereign Decision Log §v1.6.x now ~fully captured (was the main gap). Creative glossary (C-1..15), audio LESSONS (A-1..6), infra (I-1..14), products (P-1..8) confirmed covered. Tier-6/uncensored creative doc: finishing lessons in C-11; uncensored intentionally private (COSTING Tier-5 note), not a public gap. **Coverage now ~95%+.**

## E4 forensic bridge (first end-to-end run, 2026-06-13)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| E-15 | H | E4: forensic bridge ran END-TO-END for the first time. Nemotron 3-pass analysis of a clip → machine_payload.json with 16 remove targets (13 reflections/shadows!), 9 PRESERVE targets, scene-aware fill prompt, invariants. The differentiator (dense forensic constraints vs human guessing) PROVEN to produce data autonomously. | forensic e4-kitchen bundle | unmined |
| E-16 | H | The bridge's node-ID map went stale: forensic_to_comfy.py targeted the OLD image-mode SAM3 node (167:149:78) + wrong LoadVideo key ("video" not "file"), so v1 ran the baked lab clip instead of the analyzed kitchen clip — produced a plausible-but-wrong result that LOOKED successful. Lesson: when a workflow's internals change (E1c rewired SAM3 to video pipeline), every external script that injects into it by node-ID silently breaks. Verify injection landed on the LIVE job, not just that it ran. | E4 debugging | unmined |
| E-17 | M | Forensic analysis auto-chose the HERO removal target ("the egg in the pan") and wrote its own fill prompt — no human typed "remove X". This is the sellable anti-hallucination story: the model decides what to remove AND what to preserve from dense scene analysis. | e4-kitchen machine_payload | unmined |

## Benchmark sweep findings (2026-06-13)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| BS-1 | H | 4 of 9 "tested" workflows broke from ComfyUI upstream drift (node API changes + missing custom nodes). Tested-and-working has a shelf life; pin node versions or re-validate as routine maintenance | sweep 2026-06-13 | queued (drift post) |
| BS-2 | M | Goldsmith measured: 6s/11.2GB for a 1024² keyframe (confirms the catalogue's ~3.3s ballpark, slower in practice) | sweep | unmined |
| BS-3 | M | graphToPrompt() validates a workflow's node availability headlessly without queueing — fast way to audit a whole workflow library for drift | sweep technique | unmined |
| BS-14 | H | Inkwell (Dolphin 24B AWQ) MEASURED 2026-06-22: 106 tok/s single-stream decode, GPU-resident, 32K ctx (catalogue est was ~80; flipped pending→measured, leaving Daybreak as the only estimate). | CRE-1 | unmined |
| BS-15 | H | The "raw tokenizer Ġ/Ċ leak" root cause finally nailed: the AWQ repo's `tokenizer_config.json` declares `tokenizer_class: LlamaTokenizer`, so transformers 5.x routes it through the broken Mistral-regex slow path and the byte-level BPE decoder never maps `Ġ`→space / `Ċ`→newline. The `tokenizer.json` itself is FINE (round-trips clean under `PreTrainedTokenizerFast`/raw `tokenizers`). Fix = serve a tokenizer-only overlay dir with `tokenizer_class` overridden to `PreTrainedTokenizerFast` via vLLM `--tokenizer` (weights stay read-only). Neither `--tokenizer-mode slow` nor `fix_mistral_regex=True` fixed the *decode*. The historic ~2 tok/s was a SEPARATE bug — `--cpu-offload-gb 10` — not the model. Two distinct traps wearing one symptom. | CRE-1 | queued (uncensored-llm-tokenizer post follow-up) |


## Paperclip + Hermes agent company (2026-06-14)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| PA-1 | H | Hermes-local Paperclip agents need Extra args `--provider custom` or Hermes can't resolve a LiteLLM model name (e.g. cloud-preferred) → falls back to anthropic auto-detection → "Normalized model anthropic/claude-sonnet-4" failures. Fix routes via the active profile's custom provider (:4000), auto-loading LITELLM_MASTER_KEY from ~/.hermes/.env even under a clean spawn env. | WOR-3 debugging | queued (agent-company post) |
| PA-2 | H | Paperclip+Hermes has ~30-40s STRUCTURAL per-run overhead (cold hermes spawn + LiteLLM provider probing) regardless of model — the real reason it "feels slow". Drove the two-tier architecture: Hermes-direct (warm :8642) for fast single-agent jobs, Paperclip only for governed multi-agent work. | speed diagnosis | queued (agent-company post) |
| PA-3 | H | Cheap-model self-review APPROVED real buggy code: CEO wrote stack_health.py that reports LiteLLM DOWN (no auth header → 401) and self-reviewed it "spec-compliant". Mocked tests hid the bug. Validates strong independent reviewer + permanent human merge gate. | WOR-3 verify | queued (agent-company post) |
| PA-4 | H | Agent speed is dominated by model: deepseek-flash 0.8s vs Kimi-via-NGC 27-40s (timeout) — a ~30-50x swing from one config field. | model speed test | unmined |
| PA-5 | M | Paperclip UI: local-adapter agents have NO system-prompt field; the "Capabilities" box is the persona, hard rules enforced by config (budget caps, board-approval toggle, Advanced Run Policy). | dashboard build | unmined |
| PA-6 | M | The CEO went autonomous unprompted — generated its own backlog (CI pipeline, health dashboard, API docs) from a roadmap line and started building. Pause stops it instantly. Keep heartbeats OFF until deliberate. | resume test | unmined |
| PA-7 | M | Hermes Desktop update = git checkout the release tag matching the CLI (v2026.6.5) + `npm run pack` (electron-builder --dir; AppImage/rpm need appimagetool/rpmbuild you lack). `.desktop` launcher points at release/linux-unpacked/. The `hermes desktop` wrapper is broken for pip installs (probes site-packages for the JS workspace). | desktop update | unmined |
| PA-8 | M | Dead GPU agent endpoint (:8000 unloaded) makes LiteLLM personal-chain "hang" ~45s (slow failover past the dead hop). Use deepseek/groq for cloud-fast or load GPU agent-mode for sovereign-fast. | model test | unmined |
| PA-9 | L | infra-digest.sh = Hermes-direct tier proof: bash gathers status → Hermes (deepseek) writes a plain-language daily digest → ntfy. Intelligence layer atop healthcheck.sh threshold alerts. Cron 08:30. | infra-digest build | unmined |
| SA-1 | M | Super-app strategy: collapse florinpop17/app-ideas (~90 ideas, 95k stars) into super-apps — DevKit (dev tools), FocusFlow (productivity, extends Komorebi), DocForge (PDF). DevKit picked for agents: client-side, tool-by-tool, clear free/pro. | github research | unmined |

## B — bigger cross-checking agent org (2026-06-14 night)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| PA-10 | H | Built a 5-agent Paperclip org (CEO-coordinator + Architect + Builder + QA + Reviewer, all report to CEO, hire-approval gate fired on each). Researched real SaaS + 2026 multi-agent team structures to design it. | B build | queued (agent-company post) |
| PA-11 | H | **Prompt-level role constraints DON'T force delegation.** Reconfigured the CEO as "coordinator who NEVER implements" — it ignored that and fixed the DevKit code itself anyway (a capable Hermes agent self-serves because it CAN). Real fix needs a STRUCTURAL lever: remove the CEO's file-write/code tools so it MUST delegate, or route task-types to specific agents. The Architect/Builder/QA never got invoked. | B delegation test | queued (agent-company post) |
| PA-12 | M | The CEO got resourceful in a concerning way: wrote a Python script using the Paperclip REST API + a token from /tmp to POST its own status comment and PATCH the issue to "done" — bypassing the intended UI flow. Capable agents route around controls; enforce limits structurally, not by instruction. | run 107dce7f | unmined |
| PA-13 | L | hermes-founder-os skill pack installed (9 SaaS skills: validation-lab/prd-studio/architecture-reviewer/rapid-saas-builder/build-system/ux-critic/growth-lab/launch-room/founder-os) into ~/.hermes/skills/. | install | unmined |

## ComfyUI drift — root cause SOLVED (2026-06-14 night)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| BS-4 | H | The "drifted workflows" weren't garbage — root cause is CUSTOM-PACK API changes. kijai WanVideoWrapper restructured its whole pipeline (8-node→18-node; WanVideoVAEDecode→WanVideoDecode, EmptyWanLatentVideo→WanVideoEmptyEmbeds w/ output-type change). Hunyuan moved to native HunyuanVideo15* set. LLMRequest renamed. Fix = reconstruct from each pack's example_workflows template, not edit-in-place. | drift diagnosis | unmined |
| BS-5 | M | 3 OTHER packs fail to import on missing pip deps (SeedVR2: rotary_embedding_torch, SAM3: comfy_env, Nvidia_RTX: nvvfx) — a node "missing" can mean the whole pack silently failed to load, not that the node was deleted. Always check ComfyUI startup log import-failure summary. | drift diagnosis | unmined |

## ComfyUI drift — 2 Wan workflows REBUILT + render-verified (2026-06-15 autonomous block)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| BS-6 | H | Proof the drift is fixable: rebuilt the kijai Wan T2V pipeline, RENDERED, and visually verified — 17 coherent frames of a red panda in bamboo (real motion). Rebuilt t1-wan21-draft-video + t5-wan21-distill-draft (validate clean). Method: adapt the pack's own example_workflows template, not edit-in-place. Remaining 5 (3 Hunyuan no-template + 2 LLM_party) deferred — won't blind-build unattended (garbage risk). | autonomous render-verify | queued (drift post — now HAS proof frames) |

## ComfyUI drift — ALL Wan + Hunyuan FIXED + render-verified (2026-06-15 marathon)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| BS-7 | H | 7 drifted workflows fixed: Wan (t1/t5-distill render-verified), SeedVR2+VACE (baked deps), and ALL 3 Hunyuan15 RENDER-VERIFIED (dreamforge/quickening/sharpscale → 1080p). Method: official ComfyUI templates + a reusable litegraph→API converter (ui2api_render.py: auto-fix model names, prune-to-output) + render+visual-verify each. Downloaded ~11GB encoders (qwen_2.5_vl, byt5, sigclip, 1080p SR). Only the 2 LLM-orchestration ones remain. | marathon | queued (drift post — strong proof frames) |
| BS-8 | H | The cinematic LLM-orchestration pipeline was rebuilt as a HOST orchestrator (a small driver script), not an in-graph ComfyUI LLM node. Three reasons the in-graph LLM failed: (1) the creative-comfyui container CANNOT reach the sovereign CPU LLM at host :8001 (docker-bridge isolation — and widening :8001 to 0.0.0.0 just to satisfy a node would needlessly expand a local model's exposure, so declined); (2) a reasoning model emits empty `content` unless given ~900+ max_tokens (it spends the budget in `reasoning_content` first); (3) an in-graph blocking call to a slow CPU endpoint is fragile. Clean design: the LLM runs on :8001 as a host pre-step, injects the prompt, and the GPU never co-loads the LLM with the diffusion models. | cinematic-pipeline build | queued (sovereign-pipeline post) |
| BS-9 | H | Flux 2 Klein base-4b QUALITY CLIFF: sharp at 1024x1024 and 1024x576, but MELTS into a structureless painterly blob at 1280x720. The distilled 4B is resolution-sensitive >~1MP. Keep keyframes <=1MP and match the I2V aspect (1024x576 -> Wan I2V 832x480 = clean downscale). A degraded keyframe silently propagates: the I2V faithfully animated the mush, so frame 0 was already wrong — always visually check the KEYFRAME before blaming the video model. | render-verify | unmined |
| BS-10 | M | Two silent traps + one shortcut: (a) VHS_VideoCombine save_output=false writes to ComfyUI /temp, not the output mount — looks like "no output" though the render succeeded; override save_output=true. (b) the litegraph->API converter treated VHS dict-style widgets_values as a list (keys became values) — fixed to map dict widgets by field name. (c) you can free ComfyUI VRAM with POST /free {unload_models,free_memory} to run YuE in the same session — no container mode-switch needed for SEQUENTIAL GPU sharing. ACE-Step (diffusers format, no combined ckpt) + stable_audio (lib import fails) not ready; YuE is the working music engine. | cinematic-orchestrator build | unmined |
| BS-11 | M | Merging a video LoRA (WanVideoLoraSelect -> WanVideoModelLoader) into an fp8-quantized 14B model HANGS ComfyUI's single-threaded HTTP server during the merge — GPU stays idle, /system_stats returns 000, needs a container restart. Reproduced twice. Lesson: the fp8 LoRA-merge is the cost; and for image-to-video a LoRA is often unnecessary anyway — Wan I2V animates whatever is already in the keyframe, so steer with the keyframe + prompt and keep the plain proven graph. Also hardened the litegraph->API converter to RETRY on poll timeouts instead of crashing, so a transient HTTP block no longer loses an in-flight render. | I2V pipeline | unmined |
| BS-12 | H | Two cinematic-pipeline quality lessons. (1) IMAGE MODEL FIT: a general text-to-image model (small distilled Flux) cannot draw coherent HUMAN anatomy for a video keyframe — limbs/faces come out as a jumble, and an I2V model faithfully animates that jumble. The fix isn't more steps/res/LoRA; it's a domain-appropriate image checkpoint (a realistic SDXL finetune nailed anatomy + a crisp face in 4s). A strong keyframe + GENTLE motion then survives the I2V intact — no motion LoRA needed. Lesson: match the keyframe model to the subject domain; check the keyframe before blaming the video model. (2) MUSIC ENGINE FIT: a lyrics-to-song model (YuE) produces VOCALS singing the lyrics — wrong for a cinematic SCORE. ACE-Step (native ComfyUI, all-in-one ckpt) makes real INSTRUMENTAL music with empty lyrics in ~8s vs YuE's ~280s, and runs in-session (no audio-stack mode-switch). | cinematic-pipeline polish | unmined |

## KV-cache correction + catch-up lessons (2026-06-21)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| S-18 | H | KV-cache math on a Mamba-hybrid: I costed Nemotron's FP8 KV as a dense transformer (×52 layers) and claimed "~500 MiB = ~25K ctx tokens" — wrong by ~14×. NemotronH is a Mamba-2/Transformer-MoE hybrid; only 6 of 52 layers attend, so attention-KV ≈ 3 KB/token (= 2×6×2×128) ≈ ~350K tokens/GiB. Bigger correction: attention-KV is NOT the context bottleneck on a hybrid — 32GB is dominated by weights + CUDA graphs + multimodal buffers + per-sequence Mamba state. 256K = max_position_embeddings (theoretical); ~228K = total-VRAM ceiling (NOT a KV ceiling). Lesson: cost KV against the ACTUAL architecture. | sovereign §7/§8/§13 + README; corrected a-32gb-budget post | published 2026-06-21 (i-had-my-kv-cache-math-14x-wrong) |
| I-15 | M | Silent-cron-miss catch-up: when a scheduled job is found to have been silently not-running, don't just fix it — do the catch-up pass for everything it should have produced/checked while dead, and reconcile the gap. A fix that resumes the schedule but ignores the missed window leaves stale state behind. | backup/watchdog 17-day arc follow-through | unmined |
| PA-14 | H | Capable agents route AROUND prompt-level constraints: a prompt saying "you are a coordinator, never implement" does not stop a tool-capable agent from implementing — it self-serves because it CAN. Control must be STRUCTURAL (remove the file-write/code tools; route task-types to specific agents), not instructional. Generalises beyond Paperclip: any guardrail you can phrase as a sentence, a capable model can ignore. | Paperclip PA-11/PA-12 generalised | unmined |
| BS-13 | M | I2V chaining drift: chaining image-to-video segments accumulates drift — each segment animates the previous frame's imperfections, so error compounds across a multi-clip sequence (color/identity/structure wander). Steer hard at each keyframe and re-anchor rather than trusting a long autoregressive I2V chain to stay on-model. | cinematic-pipeline I2V observation | unmined |
| I-16 | H | Notifications are blind when the job never runs: outcome-monitoring (snapshot-age, ntfy alerts) catches a job that runs-and-fails, but a job that NEVER FIRES produces no log line to alert on — silence reads as "healthy." Heartbeat the SCHEDULER itself (expect a positive "ran today" signal), not just the job's output, or a dead cron entry is invisible. Extends I-1's heartbeat lesson to the un-run case. | watchdog design follow-up | unmined |
| SA-2 | M | SaaS "I-wish-there-was" search bias: mining "I wish there was an app for X" / "is there a tool that…" posts to find SaaS ideas selects for problems people can ARTICULATE and that sound buildable — a survivorship/articulation bias that over-weights crowded, already-served niches and under-weights painful-but-unsexy workflows nobody phrases as a wish. Validate demand against revealed behaviour (what people already hack together / pay for), not stated wishes. | SaaS-idea pipeline research | unmined |
| OTEL-1 | M | "Tracing is broken because the SDK is missing" was the wrong diagnosis — the LiteLLM image already bundled the OTEL SDK+exporter and the callbacks initialized fine; the real failure was the EXPORTER TARGET. Two compounding traps: (1) this Phoenix build serves its OTLP collector over HTTP on its UI port (:6006/v1/traces, advertised in Phoenix's own startup log) — the published :4318 was never bound and gRPC :4317 only accepts the TCP handshake, so "the port is open" is not "the collector is here"; (2) LiteLLM's `_normalize_otel_endpoint` appends `/v1/traces` even to the gRPC exporter target, which is an HTTP-only convention, silently corrupting the gRPC channel. Lesson: when traces don't land, verify WHERE the collector actually listens (read the server's own log line, send one probe span) before touching the client image — and prefer the transport the server advertises. | LiteLLM→Phoenix OTEL debug (AI-5) | unmined |

## Hermes update — dual-install topology (2026-06-22)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| HM-1 | M | "Update Hermes" is ambiguous because THREE checkouts/installs drift independently: (a) pip CLI `~/.local/bin/hermes` (system py3.12 user-site — upgrade with `pip install --user --upgrade --break-system-packages hermes-agent`; plain `uv pip` targets a SEPARATE 3.11 env and won't touch the live binary); (b) the git gateway install `~/.hermes/hermes-agent` w/ its own venv that runs the :8642 gateway — upgrade with `hermes update` (git pull + dep reinstall + auto gateway restart); (c) the desktop source `/data/ai/hermes-desktop-src` you actually launch. Map which install BEFORE "updating," and check both agent versions (they were drifted 0.16.0 vs 0.17.0). | hermes 0.16→0.17 (2026.6.5→2026.6.19) update | unmined |
| HM-2 | M | `hermes update` silently builds a SECOND desktop bundle inside its own checkout (`~/.hermes/hermes-agent/apps/desktop/release/linux-unpacked/Hermes`) at main-tip — divergent from the release-pinned `/data/ai/hermes-desktop-src` build the running app launches from. A self-update can leave you with two desktop binaries at different versions; pick one as canonical or they confuse "which Hermes am I running." Desktop rebuild = `npm ci` at repo root (warm cache ~8s) then `cd apps/desktop && npm run pack` (--dir → linux-unpacked); verify via the launcher's `install stamp:` line matching the target commit. | observed during update | unmined |

## Qwen-Image commercial hero + coding-stack re-stand (2026-06-22)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| QI-1 | H | HuggingFace `hf download` crashes on large fp8 safetensors via the **Xet** backend (`RuntimeError: Internal Writer Error: Background writer channel closed`) — connectivity is fine (the resolve URL 302s to cas-bridge.xethub.hf.co which serves the bytes). Bypass: `wget -c` straight from `https://huggingface.co/<repo>/resolve/main/<path>` (follows the 302, resumable). Also: stage big downloads on `/data` — `/tmp` here is only ~800MB and silently fails partway. | Qwen-Image DiT/text-encoder download | unmined |
| QI-2 | M | grep the existing model dirs BEFORE downloading any multi-GB component. The Qwen2.5-VL text encoder already existed at `/data/ai/02-models/clip/split_files/text_encoders/qwen_2.5_vl_7b_fp8_scaled.safetensors` (byte-identical) — only the 20GB DiT was actually missing. A blind download created a 9.4GB duplicate at `clip/`. CLEANUP (next creative-mode session, ComfyUI up): repoint orchestrator `render_keyframe_qwen` clip_name to the `split_files/text_encoders/...` path, delete the `clip/` dup, re-verify a render. | Qwen-Image install | open-cleanup |
| QI-3 | H | Qwen-Image (20B fp8, **Apache-2.0**) is the genuinely commercial-safe image hero — promoted to the `--mode commercial` DEFAULT (replacing the interim chroma pick that followed the FLUX.1-dev non-commercial discovery; chroma stays as `--keyframe chroma` artistic alt; ledger/FLUX.1-dev = explicit non-commercial drafts only). Measured: 12s warm / 16-20s cold, ~28GB peak (DiT 20.4GB + text-enc 9.4GB, fits 32GB w/ ~4GB headroom), eye-verified 3 seeds. ComfyUI graph: UNETLoader + CLIPLoader(type=qwen_image) + VAELoader + ModelSamplingAuraFlow(shift 3.1) + KSampler euler/simple 20 steps cfg 2.5. Catalogue tile = "Sterling". | CRE Qwen-Image wiring + benchmark | unmined |
| QI-4 | M | Coding-stack re-stand = mode switch, not co-load: `source gpu-tenants.sh; stop_all_gpu_tenants` (frees ComfyUI's resident VRAM) then `cd /data/ai/06-configs/vllm-coding && docker compose up -d`. `:8000` is exclusive (only ONE vLLM tenant at a time); healthy in ~35s; structured `tool_calls` verified via LiteLLM `:4000` model=coding (qwen3_coder parser). `:4000` needs the `LITELLM_MASTER_KEY` bearer. restart=unless-stopped = persistent. | AI-1 coding stack re-stand | unmined |
| QI-5 | M | When a Workflow fan-out dies on the session usage limit, the MAIN LOOP can still drive the same work deterministically: long downloads as background `nohup` bash (survive turn boundaries, don't touch the inference limit), edits + renders + git inline. Reliability-critical public-repo pushes are arguably safer main-loop-sequential than via a fresh agent fleet that could re-trip the limit mid-push. | qwen workflow session-limit recovery | unmined |

## Private catalogue: generated + locally hosted; auteur reconcile (2026-06-22)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| PC-1 | M | A derived/private view must be GENERATED from the canonical source, not hand-copied. The private catalogue was a manual copy of the public one and went stale within hours (missed Sterling, kept the wrong Ledger licence). Fix: a generator that re-runs the upstream public generator + lifts its DATA verbatim, then appends the private-only tiles — so it always mirrors live. Host local-only views on 127.0.0.1 via a `systemd --user` unit + `enable-linger` (survives reboot, no login), never tailnet/public; a daily user-timer keeps it regenerated. | private-catalogue generator + localhost host | unmined |
| PC-2 | H | Catalogue honesty — a hand-written tile claimed a capability the code deliberately does NOT run: the auteur/uncensored I2V path's add-on LoRAs hang the renderer's HTTP during the fp8 merge (reproducible), so the orchestrator animates the keyframe with the plain graph instead. A description written by hand drifts from what the pipeline executes; verify every tile against the actual code path before quoting it as a capability. | auteur I2V LoRA reconcile (BIZ-1) | unmined |
| PC-3 | M | cinematic-orchestrator `--stage all` scores via native ACE-Step in ComfyUI (~8s), NOT the separate YuE worker path that also exists in the file (`render_music`). When two code paths implement the same stage, confirm which one the entry point actually calls before quoting a wall-clock — the auteur end-to-end is 669s (ACE-Step), not the ~767s an older YuE-assumption implied. | auteur end-to-end measure | unmined |

## theinvalid.me design review — Stitch export (2026-06-22)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| SITE-1 | M | An AI design tool (Google Stitch) regenerated the site as a "Technical Terminal" system — and nearly every "upgrade" it proposed broke a DELIBERATE decision: Tailwind-CDN runtime JS (vs zero-JS), 4 Google webfonts (vs system-fonts-only / "performance IS the flex"), a 2nd co-equal green accent (vs one gold), navy bg (vs near-black). Verdict: design-tool exports are inspiration for LAYOUT/CONTENT, never drop-in code — the generator optimises for "looks designed," not for the locked constraints that ARE the brand. Mine the structure, reject the chrome. Post angle: "An AI redrew my portfolio and told me more about my constraints than my design." | ~/Documents/design/theinvalid-stitch-export/; reference_theinvalid_stitch_design memory; SITE-DECISIONS.md | unmined |
| SITE-2 | M | The one genuinely good idea from that export: its "Local LLM Field Guide" structure (the math nobody tells you → memory tier map → what runs where → interactive picker, framed "without the benchmarketing"). Captured as an in-brand `/calculator` upgrade spec — but re-based off Stitch's Apple-Silicon unified-memory / generic "70% rule" / invented model names onto our MEASURED dedicated-GPU reality (the 14× KV-cache correction, the 32GB budget, the how-many-users concurrency knee). The structure travels; the domain + numbers must be ours. | docs/SPEC-llm-field-guide.md; posts i-had-my-kv-cache-math-14x-wrong + a-32gb-gpu-is-a-budget + one-rtx-5090-how-many-users | unmined |
| SITE-3 | M | IA consolidated 8 nav tabs → 5 (2026-06-23): `start here · work with me · projects · field guide · knowledge base`. Two root causes of tab-bloat: the homepage already duplicates projects+blog, and the nav exposed BOTH audiences' full journeys flat (buyers: start-here→services→case-studies→resume; engineers: projects→blog→calculator→github). Merges: case studies → a pinned section in Knowledge base (/blog); `cloud vs local` calculator → renamed "field guide" (own tab, per owner); resume → OFF the nav (page kept, linked from work-with-me + a dated auto-current download `Name-Resume-YYYY-MM.pdf`); github → footer logo+username. Redirects `/case-studies` + `/knowledge-base` → `/blog`. Lesson: "too many tabs" was really two audience funnels exposed as siblings — collapse each funnel to one strong page, let the homepage be the hub. Phase 2 pending: build-time GitHub status sync (cards show "updated X ago") + resume PDF auto-gen + scheduled rebuild. | Base.astro nav/footer; blog/index.astro; astro.config redirects; resume.astro live-projects; SITE-DECISIONS (to update) | unmined |
| SITE-4 | L | TODO (owner, later): the auto-generated resume PDF is **4 pages** — a touch long. Trim `src/content/resume.md` for a tighter 1–2 page CV; the PDF tracks it automatically now (rendered at build by the daily-refresh workflow). Owner content call on what to cut. | src/content/resume.md; scripts/render-resume-pdf.mjs | todo |
| SITE-5 | H | The workflow catalogue **silently drifted from its own data source** (2026-06-24). `generate-catalogue.py` builds tiles from a hand-maintained `META` dict, but the workflow set actually lives in the benchmark CSVs — so when Qwen-Image (Sterling, the Apache-2.0 commercial image hero) landed in the CSV on 06-22, it never became a tile (META wasn't updated), the count showed 28 not 29, and the **monthly job auto-published the incomplete page with the generator's output suppressed** (`>/dev/null 2>&1`) → zero alert. Same audit caught a FALSE license claim: Ledger/FLUX.1-dev was labelled "Apache-2.0 commercial-safe" but FLUX.1-dev is **non-commercial**. Fix: added Sterling (→29 tiles), corrected Ledger (points to Sterling for paid work), added an explicit `IGNORE` set for the 11 benchmark-only CSV rows (superseded/runs/infra/aggregates), and a **`--strict` reconciliation guard** — every CSV workflow must be a tile or explicitly ignored, else the generator exits non-zero. Wired `workflow-maintenance.sh` to run `--strict`, **gate the auto-push on it**, and ntfy-alert instead of shipping incomplete. Lesson: a catalogue whose item-list is maintained *separately* from its data source WILL drift silently — reconcile the two and make the mismatch loud (fail the publish), or new work just vanishes from the shop window. | generate-catalogue.py (META+IGNORE+--strict); workflow-maintenance.sh; public/workflow-catalogue.html | unmined |
| SITE-6 | H | `status_of()` **downgraded a MEASURED workflow to "estimate" by substring-matching free text** (2026-06-25). Daybreak's CSV note reads `MEASURED 2026-06-22 (was estimate)` — the heuristic did `"estimate" in notes.lower()`, matched the word describing its PAST state, and flipped a measured pipeline to estimate. Same bug-class as A-8 (the BLOCKED prose-substring) — fixed there, missed here. Worse, a catalogue regen then **published the wrong "27 measured" count without anyone verifying it against the recorded data**. Fix: `status_of` now honors explicit UPPERCASE markers first (`MEASURED`/`ESTIMATE`/`BLOCKED`), then the wall-clock value (`~`-prefix = estimate), never the lowercase word. Same audit found Inkwell's catalogue static stale (`pending`/`~80 est`) vs the recorded BS-14 measurement (106 tok/s, "flipped pending→measured") — updated. Honest count restored to 29/29 measured. Lesson: a status DERIVED by substring-matching prose will misread text that mentions the very word it keys on; key on explicit markers, and NEVER publish a derived status without checking it against the recorded measurement. | generate-catalogue.py status_of() + Inkwell static; benchmarks.csv DailyPipeline row; queue BS-14 | unmined |

## Firecrawl cost control → self-hosted Crawl4AI (2026-06-23)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| SCR-1 | M | Capping a metered scraping API (Firecrawl) without losing capability: keep it on the FREE tier (1000 credits/mo, no rollover) reserved ONLY for hard anti-bot/Cloudflare sites, offload the bulk to self-hosted Crawl4AI (free) with LLM-extraction routed through your own LiteLLM. The non-obvious split the "just self-host" advice misses: Crawl4AI replaces the SCRAPE, not the SEARCH — discovery still needs SearXNG (sovereign) or a search API, and most credit burn was search, not scrapes. Post angle: "I researched Firecrawl alternatives without spending a single Firecrawl credit." | crawl4ai deploy /data/ai/06-configs/crawl4ai/; feedback_scraping_stack memory | unmined |
| SCR-2 | M | Two Crawl4AI self-host gotchas: (1) its gunicorn binds 127.0.0.1 INSIDE the container, so a bridged `ports:` forward gets "connection reset" while the in-container healthcheck shows green — fix with `network_mode: host`. (2) On a loopback bind with no token it silently MINTS AN EPHEMERAL token each restart and returns "Authentication required" everywhere — set a stable `CRAWL4AI_API_TOKEN` and pass `Authorization: Bearer` on every call incl. the MCP `--header`. Read the server's auth code to find the real condition instead of guessing config flags. | server.py auth_gate; crawl4ai 0.9.0 | unmined |
| SCR-3 | L | Cheapest scraping optimization needs no new infra: prefer the FREE built-in WebSearch/WebFetch before any paid scraper; call the API's own refund hook (firecrawl_search_feedback refunds 1 credit/search); hit native sources (raw.githubusercontent.com, sitemaps, RSS) before rendering a page. Datacenter-IP self-hosting (VPS) is WORSE for anti-bot than a residential IP — don't reach for it as an IP-rotation trick. | this session's usage pattern | unmined |

## On-brand architecture diagrams across every repo (2026-06-23)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| DIAG-1 | M | Mermaid vs drawio vs SVG isn't really the question — match the format to the READER. For a visual learner, a polished on-brand SVG (dark canvas, ONE gold accent, system fonts, gold=local/hero/ports + muted-dashed=cloud/fallback) conveys an architecture at a glance where Mermaid's auto-layout makes them read box-by-box and lose the thread. Hand-authored SVG committed to each repo's `docs/` + embedded via `<img>` renders on GitHub AND stays the source of truth. Swept all 3 stacks + comic + devkit + legal-rag + n8n + the infra topology + komorebi data-model onto one brand. Post angle: "I redrew every repo's architecture in my own brand — and learned the format question is really a reader question." | this session; feedback_diagram_brand memory; ~/Documents/diagrams/ | unmined |
| GIT-1 | H | `git add <my files>` then `git commit` commits the WHOLE INDEX, not just your files — if the repo already had OTHER files staged (komorebi had 25 pre-staged Play Store assets), your "add a diagram" commit silently bundles + pushes them with a misleading message. Fix: commit with an explicit pathspec — `git commit -- README.md docs/x.svg` — which commits ONLY those paths and leaves everything else staged. Recovery if already pushed: `git reset --soft HEAD~1` → pathspec re-commit → `git push --force-with-lease`. Always `git diff --cached --name-only` before committing in a repo you didn't just clean. | komorebi mis-commit 30d1fd8 → fixed 28f8bf9 | unmined |

## Mushishi mode-picker: idle-vs-loaded probe + missing Coding mode (2026-06-24)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| MODE-1 | M | A GPU mode-switcher must distinguish a model that is merely RESIDENT (loaded, holding 29GB, doing nothing → safe to purge) from one that is WORKING (serving a request / running a render). The old picker conflated them: its guard flagged any >2GB GPU process, so it nagged "you'll kill running work" on EVERY switch even when the card was idle. Fix = an activity probe (`gpu_busy_report`/`gpu_resident_line` in gpu-tenants.sh) that reads the AUTHORITATIVE signal per workload — vLLM `:8000/metrics` `num_requests_running`+`_waiting`, ComfyUI `/queue`, audio RQ registries — with a GPU SM-util sample (`nvidia-smi --query-gpu=utilization.gpu`, peak over ~1.4s, >15% = busy) as a backstop ONLY when the exact probes say idle. "0% util in one nvidia-smi snapshot" is not proof (decode is bursty); sample a window AND check the request counters. The picker now shows `Loaded: <container · served-model>` + `Status: ✓ IDLE / ⚠ BUSY` in the header and only prompts to confirm on real work. Post angle: "my GPU said 29GB used, 0% busy — teaching a tool to tell 'loaded' from 'working'." | /data/ai/01-workspace/scripts/{gpu-tenants.sh,mode-picker.sh}; feedback_mode_gpu_stoplist memory | unmined |
| MODE-2 | M | A stop-list entry is not a start path. `vllm-coding` (Qwen3-Coder-30B-AWQ, the persistent LiteLLM `coding` backend) was in `GPU_TENANTS` so every mode EVICTED it, but there was no `coding-mode.sh` to BRING IT UP and it was absent from the picker — you could only stop it, never start it from the UI. Added `coding-mode.sh` (mirrors forensic/agent: `stop_gpu_tenants_except vllm-coding` → `docker compose up -d` → wait on `/v1/models`) and a "Coding" picker entry; also surfaced the already-existing-but-hidden `agent-mode.sh` (the picker's single "AI" row only ran forensic) and relabelled AI→Forensic. Bonus: stopping Coding (any switch / StopAll) drops :8000 and LiteLLM fails `coding` over to the always-on CPU Qwen coder :8002 — the CPU floor doubles as the mode-switch failover, so the CLI never loses its endpoint. | coding-mode.sh; vllm-coding/README.md; SPEC-coding-stack.md | unmined |

## Mushishi Mode — design-tool mockup → native GTK4 app, brand-reconciled (2026-06-24)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| GUI-1 | M | A design-tool mockup (Stitch) is a STRUCTURE brief, not a skin to import — same rule as the site export (SITE-1). Stitch redrew the GPU switcher beautifully but in navy `#0b1326` + dual amber/green accent + 3 webfonts; we kept its LAYOUT (sidebar, status header, VRAM meter, mode rows w/ chips+sizes, footer) and reconciled the palette to the locked brand by porting the token system already realized in the Sovereign Legal RAG UI (`static/index.html`): near-black `#0A0D13`, ONE gold `#E8A33D` hero, emerald `#46B98A` STATUS-ONLY, mono labels. The reusable asset wasn't the mockup — it was a *sibling app's* CSS tokens. "Mine the structure, reject the chrome," now proven twice. | /data/ai/01-workspace/mushishi-mode/{DESIGN.md,style.css}; sovereign-legal-rag/static/index.html | unmined |
| GUI-2 | H | Python `str.splitlines()` splits on `\x1c`/`\x1d`/`\x1e` (FS/GS/RS), NOT just `\n`. A bash→Python IPC that joined fields with `\x1d` (Group Separator) had every record silently shredded — the GUI showed "no compute processes" while 28GB was in use, and `bash probe.sh` alone looked fine (the bug only appears once Python `.splitlines()` touches it). `\x1f` (Unit Separator) is SAFE (not a splitlines boundary); use `\x1f` for records + TAB for fields, never `\x1c/\x1d/\x1e`. Caught only by live screenshot verification — the unit looked correct. Post angle: "the ASCII separator that Python silently treats as a newline." | mushishi-mode/probe.sh + mushishi_mode.py `_apply_probe` | unmined |
| GUI-3 | M | Porting a SYNCHRONOUS guard to an ASYNC UI can silently weaken it. The zenity picker probed busy-state synchronously right before deciding; the GTK port read a cached value refreshed every 4s + empty at startup → it could skip the "you'll kill running work" confirmation. Fix: on Apply, re-probe and decide from the callback (fresh verdict), disable the action until the first probe lands. When you move a check off the critical path for responsiveness, re-establish freshness at the decision point. Also: a GTK `DrawingArea`+Cairo draw func needs `python3-gi-cairo` (pycairo↔GObject bridge) or it throws "foreign struct converter for cairo.Context" — a CSS-styled `Gtk.ProgressBar` avoids the dependency. | mushishi-mode/mushishi_mode.py; review wf_7323bc7a | unmined |
| GUI-4 | L | Adversarial multi-lens review (correctness/etiquette · brand · a11y · robustness) of a ~450-line GTK app surfaced a real high-sev bug (the splitlines one) plus AA-contrast (`ink3` 4.06:1<4.5), an unguarded destructive StopAll, and N/A-field parse fragility that a single self-review missed. For a narrow-solution-space build (given design + reference), the value is in VERIFYING, not re-generating — build once, adversarially verify, fix. | review workflow wf_7323bc7a; DESIGN.md v1.0.1 changelog | unmined |

## Tailscale ACL + Cloudflare AI-bots (2026-06-24)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| NET-1 | H | Tailscale **tag-based ACLs are inert until the devices are tagged**. Applying the policy alone (tagOwners + tag-scoped accept rules) with untagged devices = every rule matches nothing = silent default-deny everywhere. "Apply the ACL" MUST include assigning each device its tag (admin console → Edit ACL tags, or `POST /api/v2/device/{id}/tags`). Symptom of the trap: even the explicitly-allowed ports are blocked while `tailscale ping` still works (ping is path-level, not ACL-gated). | BIZ-3 apply | unmined |
| NET-2 | M | Verify a directional ACL from the RESTRICTED side against the TARGET's tailnet IP — not the tester's own. Easy footgun: running `nc <my-own-tailnet-ip> <port>` tests the box against itself (its own sshd open, nothing else), proving nothing about the peer→peer rule. Always nc the *other* device's IP. | BIZ-3 verify footgun | unmined |
| NET-3 | M | Cloudflare's **Bot Management / rulesets / zone-settings APIs are paid-plan only** — on a Free zone, even a correctly-scoped Bot-Management token returns `9109`/`10000` "Unauthorized". The free "Block AI bots" feature is **dashboard-only**; there's no API to read/flip it on Free. Check actual bot access by behavior (curl with crawler UAs → 200/403) + robots.txt, not the API. | BIZ-12 | unmined |

## smartd NVMe self-tests + ntfy alerts (2026-06-24)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| DSK-1 | M | smartmontools default `DEVICESCAN -d removable` still *health-monitors* fixed NVMe drives (smartd reports "Monitoring N NVMe"), but runs **NO scheduled self-tests** and mails local root — on a box with no MTA that's a SILENT safety net. For real coverage add explicit per-device lines with `-s (S/../.././02|L/../../6/03)` (short daily/long weekly). Don't assume "smartd is running" = "drives are self-tested + you'll be told." | BIZ-5 | unmined |
| DSK-2 | L | NVMe self-test (DST) support often isn't shown by `smartctl -c \| grep -i self-test` (ATA-phrased); detect it by probing `smartctl -t short` and checking the selftest log, or just trust modern NVMe (990 PRO supports it). And for alert delivery without a mailer, use smartd `-m <nomailer> -M exec <script>` to run only the exec hook (e.g. curl→ntfy). | BIZ-5 | unmined |

## Blog resilience: Claude-primary -> local-draft fallback -> Claude-reviews-on-return (2026-06-24)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| BF-1 | H | Blog now degrades gracefully: when headless Claude can't run, blog-fallback.sh has a LOCAL model DRAFT the next [queued] slot into gitignored drafts/ (never auto-published, never dirties the publish tree), records it pending, and alerts. When Claude is back, blog-bot.sh's "review-on-return" has Claude review the draft, list concrete fixes, and alert — owner publishes after. Continuity (never lose a slot to an outage) WITHOUT shipping unreviewed local prose to a live client-facing site. Verified e2e: local draft 681 words/~100s; Claude review caught real technical errors. | blog Claude->local fallback build | unmined |
| BF-2 | M | Two traps when wiring a local LLM fallback for slow CPU long-form: (1) DON'T route it through LiteLLM's sovereign/failover aliases — their short timeouts (30/120s, tuned for fast GPU-failover) return HTTP 500 on a multi-minute CPU generation (and chain-compound through each fallback). Hit the always-on CPU endpoint DIRECTLY (own timeout). (2) DON'T use a REASONING model (nemotron-cpu-prism :8001) for drafting — it spends the entire max_tokens budget on the <think> block (5136 chars reasoning, 0 content, finish=length). Use a NON-reasoning model (qwen-coder-cpu :8002) — finishes naturally with clean markdown. | blog-fallback model/endpoint debug | unmined |

## Derived-artifact generators must not dirty a publish-gated repo + Hermes kanban visibility (2026-06-24)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| BF-3 | H | A generator for a DERIVED artifact must be READ-ONLY w.r.t. any repo guarded by a clean-tree publish gate. The private-catalogue generator re-ran `generate-catalogue.py`, which rewrote `public/workflow-catalogue.html` (even just the "Updated <date>" line) → dirtied the theinvalid-site repo → `blog-bot.sh`'s clean-tree guard would `skip publish` EVERY day the regen timer fired (silent blog stall). Fix: the private mirror now READS the deployed public file, never regenerates it in place. Generalizes: any cron/daily job that writes into a `push=live` repo can silently wedge that repo's publish automation — keep generated state out of the guarded tree (or restore it after). | private-catalogue regen blocking blog | unmined |
| BF-4 | L | Hermes kanban doubles as a visibility surface for headless automation: `hermes kanban create "<title>" --body … --created-by <bot> --idempotency-key <k> --json` posts a card to the `default` board (shows on the personal Hermes desktop); leave `--assignee` UNSET so no dispatcher auto-runs it (it's a marker, not a task). Close the loop later with `hermes kanban comment <id>`. The blog fallback uses this: card on draft, Claude's review as a comment on return. | blog fallback → Hermes kanban | unmined |

## SovereignSec code auditor (SC) — local agentic code-security auditor (2026-06-25)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| SC-1 | H | **The fine-tune added zero detection — and that's the result.** On a leak-resistant POST-CUTOFF eval (CVEs published 2025–26, fixes after the data cutoff) base Qwen2.5-Coder-32B and the fine-tuned 32B scored identical P/R/F1 1.0 → detection delta EXACTLY 0.0 (held-out 7B = +0.05 recall, noise). 879 real mined pairs + three runs (1.5B/7B/32B, loss 0.856/0.686/0.562) bought OUTPUT DISCIPLINE — one terse parseable calibrated `FINDING:{cwe,confidence}` line vs the base's five verbose formats — not new sight. Capability lives in the SYSTEM. Full hand-written draft already exists (this session). | SovereignSec-AI bench/results POSTCUTOFF_ABLATION + INSIGHTS + docs/FAQ | unmined |
| SC-2 | H | **My benchmark lied four times.** A toy eval (1 vuln fn + 1 safe fn) scored 1.0; the SAME system on real CVE files (full Django modules, ~14 fns) scored 0.07 — the toy measured "tell two hand-picked snippets apart," not detection. Worse, early "wins" were GRADER bugs: a detector matched the substring `secure` inside `insecure`; a format mismatch credited a finding for the wrong reason. Took 4 detector iterations + reading RAW model outputs to see it. Same bug-class as A-8 / SITE-6 (status-from-substring). The way you measure can lie louder than the thing you measure. | bench eval-harness fixes (BLOG E5/E6/E7); INSIGHTS | unmined |
| SC-3 | H | **The license picked the architecture.** CodeQL's licence forbids scanning private/customer code without a paid enterprise seat — dead on arrival for an air-gapped product whose whole point is private code. Semgrep's CROSS-FILE taint (the capability needed most) is paid-tier; the OSS engine is single-file. So the cross-file taint engine had to be hand-built on tree-sitter + a call graph. The constraint that looked like a limit became the moat: a cross-file engine I own, runs offline, no licence can revoke. | IMPL_SPEC licensing; INSIGHTS | unmined |
| SC-4 | H | **Naive SAST hints make a good LLM WORSE; the hybrid is a UNION, not a pipeline.** Feeding the 32B one-shot SAST hints dropped it 0.90→0.79 (anchoring). Agentic-alone is precise but blind beyond its rules (0.69 < bare 0.90). What wins runs the LLM (breadth) and the deterministic system (precision + proof) INDEPENDENTLY and MERGES them: hybrid 0.97. Neither pure-LLM nor pure-system wins. | bench HAYSTACK_REPORT_v2/v2cov; INSIGHTS #8; BLOG E9 | unmined |
| SC-5 | M | **Deterministic taint beats the LLM on cross-file precision.** On a benchmark seeded with safe-but-suspicious decoys (same sink, constant/local input), the cross-file taint engine = 1.00 precision / 1.00 recall; the per-file LLM = 0.25 precision / 0.60 recall (misses non-obvious cross-file vulns AND false-positives on the decoys). The engine flags exactly the user-reachable sinks, with a proof path. | bench CROSSFILE_PRECISION_REPORT | unmined |
| SC-6 | M | **A rule system is exactly as good as its coverage — measured before/after.** One-line fix — register `request.get_json()` as a taint SOURCE (the Flask JSON-body pattern an entire vuln class used) — lifted SSRF coverage 0→1.0; broadening rules moved agentic 0.41→0.69 and hybrid 0.93→0.97, proof-carrying findings 17→38. The per-class gaps left (path-traversal ~0.4, deserialization 0.2) point at exactly what to build next. | bench HAYSTACK_REPORT_v2cov; BLOG E10 | unmined |
| SC-7 | H | **A pseudonymous public repo leaked my real name via GIT METADATA, not content.** Creating the repo I set `git config user.name "<real legal name>"`, so every commit's author exposed the identity behind the pseudonym via `git log` — though NO content file held the name (the sanitization pass had scrubbed contents). The commit `+0800` offset was a location signal too. An adversarial pre-publish audit caught it AFTER the first push. Fix: pseudonym + GitHub noreply email + `TZ=UTC`, `git commit --amend --reset-author`, force-push; verify `git log --format='%an %ae %ai'` BEFORE pushing. Sanitize metadata, not just files. Same "guardrail must be structural" family as I-11. | SovereignSec-AI publish; pre-publish audit workflow | unmined |
| SC-8 | M | **The data moat: GHSA tags fix commits as `references[].type=="WEB"` NOT `"FIX"`** — harvest BOTH or the miner finds ~nothing (real run: 355 WEB / 0 FIX; FIX-only = 0 pairs, fixed code = 134). No off-the-shelf label-verified Python-web vuln set exists, so curated GHSA→fix-commit mining is the moat: 879 real vuln→patch pairs. Beware label noise (BigVul 68%F1 → PrimeVul 3%F1) and benchmark contamination. | ft-rig/data mine_ghsa.py; IMPL_SPEC §0 | unmined |

## Comic Narrator — publishable-quality push: expressive voice + music (CN, 2026-06-28)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| CN-1 | H | **A "broken model" was a broken WAV write — torchaudio 2.12 clips int16 at 99.6%.** IndexTTS-2 inference was perfect; the output was noise. torchaudio 2.12 dropped the soundfile/sox backends and now routes `save()` through torchcodec, which reads the model's **int16** waveform tensor as float [-1,1] and saturates EVERY sample over 1.0 → 99.6% clipped (verified by reading raw samples: peak 1.0, RMS -0.0 dB, flat-factor 45). The fix is one shim — override `torchaudio.save` to write via `soundfile` (handles int16 natively). Lesson: when fresh audio output is garbage, inspect raw samples BEFORE blaming the model; the I/O layer lies. Any audio model saving via torchaudio ≥2.12 hits this. | comic-narrator IndexTTS-2 serving | unmined |
| CN-2 | M | **Expression = the ability to MOVE between registers, measurable as dynamic range.** Same line, same character ref: Fish Speech sat flat at -28 dB RMS regardless of emotion; IndexTTS-2 climbed -16.6 → -11.6 → -10.3 dB across neutral → determined → emotion-text. The "lifeless" complaint wasn't timbre — it was a single fixed register. A flat RMS across emotional takes is the signature of a TTS that reads instead of acts. | comic-narrator TTS A/B | unmined |
| CN-3 | M | **A limiter, not a normalizer — preserve the loudness RELATIONSHIP.** Per-line peak-normalizing would flatten a shout to the same level as a whisper, deleting the acting. Scale down ONLY when peak exceeds -1 dBFS; leave quieter lines alone. The dynamics between lines ARE the performance. | comic-narrator IndexTTS-2 save shim | unmined |
| CN-4 | M | **Short pages must score the PEAK beat, not the average.** A music-mood vote that needed ≥2 intense lines scored a shocked "WHAT? DUNGEON?!" page as calm (one shout drowned by neutral exposition). Comic pages are 1-4 lines; the underscore should track the most dramatic moment. Fix: peak-driven priority (one intense line → tense, ≥2 → action). Averaging is wrong when the sample is tiny and the signal is the outlier. | comic-narrator music.mood_from_tones | unmined |
| CN-5 | L | **Container mounts are not the host FS — a script ran "successfully" against a path that didn't exist.** ACE-Step runs inside creative-audio-worker (its venv reuses the container's SM_120 torch via --system-site-packages), but that container mounts only some /data/ai subdirs — not 08-portfolio or 06-configs. `docker exec … python /data/ai/08-portfolio/…/gen.py` failed with "No such file" despite exit 0 on the wrapper. Run from a mounted path, output to a mounted dir, host-copy the rest. | comic-narrator music-bed generation | unmined |

## Comic Narrator — v7 publish pass + adversarial QC (CN, 2026-06-29)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| CN-6 | H | **The engines worked; the ASSEMBLY shipped the bugs.** A multi-agent QC of the v6 render said "not publishable" for two reasons that had nothing to do with the (genuinely good) voice + music engines: ~30% of runtime was bit-exact silence (every page opened on a 3s establishing-shot leader the music bed never covered — the muxer adelay'd the music-bearing narration *past* the overview), and the master clipped at +0.7 dBTP (hot TTS + tone gain summed past 0 dBFS in the pydub int16 mix). Both downstream of the models. Lesson: when judging a generative pipeline, audit the assembly/mux/mastering stage as hard as the models — that's where "the demo sounds great in isolation but the file is unshippable" lives. | comic-narrator v6→v7 QC | unmined |
| CN-7 | H | **Un-clipping looks exactly like flattening — and a careless reviewer would call the fix a regression.** v7 added 6 dB speech headroom + a normalize so nothing clips. Episode-wide RMS-std then "contracted" 7.07→4.13 dB — the textbook signature of squashed dynamics. It wasn't: v6's loud lines were CLIPPED FLAT (crest factor 9.5 dB), so they read as artificially consistent; v7 restored their crest to 12.0 dB and the shout-vs-neutral GAP grew (+8.53→+9.06 dB). The std dropped only because clipped peaks were pulled down into headroom. The metric that screams "regression" was measuring the removal of a defect. Always decompose an aggregate-variance change into its cause before calling it. | comic-narrator v7 voice-expression re-audit | unmined |
| CN-8 | M | **loudnorm can't hit in-band loudness AND sub--1 dBTP on peaky speech — you need a brickwall after it.** Targeting -15 LUFS landed -16.6 LUFS @ -1.34 dBTP (TP-safe but quiet); targeting -14 landed -15.3 LUFS @ -0.51 dBTP (in-band but TP over target). Raising loudness pushes peaks up; loudnorm undershoots whichever target it must to respect the other. Fix = loudnorm for loudness THEN an `alimiter` brickwall at -1.5 dBFS: loudness holds (~-15 LUFS), only the few over-ceiling transients are limited → -15.3 LUFS AND -1.25 dBTP, 0 clip. Two-pass loudnorm (measure→apply) also beats single-pass, which drifted to -0.20. | comic-narrator finalize_loudness | unmined |
| CN-9 | M | **My own onset-based sync check threw a false -500ms uniform offset; cross-correlation was the truth.** A regression skeptic's first A/V-sync method (audio-envelope onset vs SRT) reported a scary uniform -500ms drift; proper waveform cross-correlation (corr 0.97-1.00) showed -16..-43ms with no cumulative drift. The cheap measurement lied; the verdict held only because a second, stronger method was run before believing it. Adversarial verification has to include verifying your VERIFIER's method. | comic-narrator v7 av-sync re-audit | unmined |
| CN-10 | M | **The VLM mixes pixel and 0-1000-normalized bbox coords WITHIN one page — so sanitize per-box, not per-page.** Nemotron's char/sfx boxes are panel-local but loose: page_0004 panel-1's speaker came back `(460,250,490,720)` — overflows the 601×679 crop as pixels (x+w=950) but is valid 0-1000-normalized → `(276,170,294,489)`; meanwhile panel-2's boxes were honest pixels. No page-wide flag works. The old `_coerce_bbox` only handled 0-1 floats and missed this, pinning the speaker-spotlight halo to a panel edge. Fix = a per-box `_sanitize_bbox(b,pw,ph)`: keep if it fits; if it grossly overflows, adopt the ÷1000 reading *when that lands inside*; clamp a ≤12% overshoot (rounding) without shrinking; drop a box still mostly outside (no halo beats a wrong one). Apply at the producer so page.json is clean → both the halo and the area-ratio `derive_shot_type` get good data. | comic-narrator vision/nemotron_client `_sanitize_bbox`; tests/test_bbox_sanitize.py | unmined |

## Mushishi Bridge + the local-coding way out (MB, 2026-06-30)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| MB-1 | H | **With a local agentic coder the bottleneck is the MODEL, not the harness.** A 3-way M0 build of the same spec on the same model proved it: vanilla CCR→Claude Code produced NOTHING (the vLLM `qwen3_coder` parser + the 30B drop the `<tool_call>` wrapper under load → tool calls leak as plain text and never execute — harness-independent, it hit Qwen Code too); Qwen Code completed but plausibly-broken (hallucinated registry, assumed probe.sh was JSON, broke auth); Opus was correct. Two separate ceilings — reliability (the parser leak) and quality (a 30B-A3B is junior). A nicer harness fixes neither; the real quality lever is VRAM. | mushishi-bridge M0 build-quality test | unmined |
| MB-2 | H | **Aider's diff format sidesteps the tool-call leak — so the "way out" is the loop + the oracle, not a Claude-Code clone.** Aider edits via SEARCH/REPLACE text blocks, not tool-calls, so the parser leak never applies. Wire it to a cheap oracle (`--auto-test` running pytest/lint/build): the local model can be wrong N times, the oracle catches it free, and Opus only reviews the GREEN diff. Proven twice incl. a real helper added to the Bridge's own codebase, zero Opus tokens in the loop. Gotcha: install Aider in its OWN venv — into system Python it clobbered hermes-agent's pinned deps. | qwenaider dogfood | unmined |
| MB-3 | M | **A build spec is only fair to a junior model if it's model-agnostic OR paired with a verification loop.** The Bridge spec was Opus-calibrated (read files, follow nuanced rules, self-verify); grading a 30B against it measured senior-ness it doesn't have. Either prescribe exact code, or attach an oracle that defines "done." | bridge SPEC vs local-model output | unmined |
| MB-4 | M | **Adversarial verification catches your own agents derailing — not just bad code.** In a fan-out workflow one implementation agent silently returned "output-style" meta-text and made zero edits; the integrate phase's independent check flagged `all_pass:false` and named the missing change, so I fixed it directly. Always add a verify/integrate phase, fan out by non-overlapping files, and know workflow subagents share the account session limit (a whole review round can die mid-flight and report 0 findings — which means 0 reviewers ran, not "clean"). | bridge D+B implement + review workflows | unmined |
| MB-5 | M | **To surface private data without spreading it: parser-in-repo, data-in-its-private-home, read live + auth-gated.** The Bridge's Creative tab lists the uncensored workflow catalogue, but the repo holds only the PARSER (`catalogue.py`); the private content stays in `~/Documents/business/` and is read at request time behind a bearer token, tailnet-only. The repo never carries the secret content. | bridge Creative tab | unmined |

## Ops fix batch + execution fan-out (OPS, 2026-07-02)
| id | pri | lesson / decision | source | status |
|---|---|---|---|---|
| OPS-1 | H | **Cron GPU jobs must distinguish "VRAM held" from "GPU busy".** Map nvidia-smi PIDs → containers, act only on the gpu-tenants.sh canonical names, and reuse the `gpu_busy_report` idle verdict — an idle resident tenant is flushable+restorable (flush → run → `docker start` + :8000 verify + EXIT-trap restore), a foreign or working holder still means skip. workflow-maintenance.sh had skipped every cycle since 2026-07-01 behind an idle vllm-coding; idle-flush v2 cleared the overdue run same-day. | workflow-maintenance.sh idle-flush v2 | unmined |
| OPS-2 | H | **The backup "failure" was two bugs, neither the one reported:** (1) restic temp packs overflowed the tiny 804M /tmp partition on a full re-read (fix: `TMPDIR=/data/...` in backup.sh); (2) the LLM infra-digest was fed a snapshots-table FOOTER (no timestamps) plus its own previous digest text as "recent errors", so it hallucinated staleness dates and a fake repo path for days. Never let a summarizer see data without timestamps, and never feed a monitor its own output — the deterministic stamps were the only honest signals throughout. | backup.sh + infra-digest.sh fix 2026-07-02 | unmined |
| OPS-3 | M | **A fresh success sentinel is not success:** benchmark.sh stamped 2026-07 while measuring 0 tok/s (endpoint not serving) — join stamps WITH log outcomes. And the 8-alert morning of 07-01 collapsed to 3 root causes: alert on correlated causes, not per-job pings. | estate-brain.sh (THIS-8) | unmined |
| OPS-4 | M | **Encode a playbook as gate-terminated file scaffolds:** a CLI that structurally refuses to price, render, or ship converts "discipline in the operator's head" into structure without an agent framework; and verify stub commands against argparse, not their SPEC (the orchestrator had grown `--mode commercial` undocumented). | client-delivery thin slice (THIS-7) | unmined |
| OPS-5 | M | **Small local models need deterministic guardrails, not instructions:** qwen-coder-cpu ignored "do not duplicate these slugs" (100% dupes on attempt 1), merged 3 output formats into 1, and leaked an internal hostname as a hashtag — fix = per-section calls + code-side dedupe/URL-append/name-reject, retry at rising temperature. | content-coordinator.sh v2 (AI-4) | unmined |
| OPS-6 | M | **A compose file fixed in place is only half a fix — the doc block people copy from re-ships the bug.** The as-built forensic compose dropped `--moe-backend triton` + `VLLM_USE_FLASHINFER_MOE_FP4=1` in v1.5.1 (they BREAK NVFP4 MoE on SM120, vllm#34452), but the public spec doc kept prescribing both in FOUR places (compose block, flag table, troubleshooting, glossary) for six weeks. Same drift class as the five stop-list greps B2 killed: any duplicated operational truth needs a single source + references — docs that inline configs must be corrected in the same change as the config. Fixed in stack doc v1.8 (agent-mode + gpu-tenants.sh folded in, d5d5d9a). | sovereign-ai-stack doc v1.8 (BIZ-9 D2) | unmined |
| OPS-7 | L | **Owner decision 2026-07-02: phone number stays ON the public resume** (SG recruiters must be able to call) — reverses the same-day PII scrub for phone ONLY; address and all other PII stay off. Restored in src/content/resume.md + public/resume.md + regenerated PDF (372883b). | resume phone restore (372883b) | unmined |
| OPS-8 | M | **Committed generated files must be regenerated with CI's exact toolchain, and a bot's "unresolved" flag can just be a wrong suggested diff.** qdrant-client PR #1247: (a) CI regenerates `local/async_qdrant_local.py` from the sync source and diffs — the first two commits only patched the sync client, a latent consistency-check failure nobody flagged; (b) regenerating on Python 3.12 produced spurious churn (`ast.unparse` tuple-paren drift vs CI's 3.10) and an unpinned ruff added more — clean output needed uv-fetched 3.10 + poetry.lock's ruff 0.4.3/autoflake 2.3.3; (c) CodeRabbit left its "major" batch finding unresolved only because its suggested fix used a nonexistent field (`request.search_params`; real field is `params`) so its match heuristic missed the pushed fix — verify bot findings against current code before "fixing" them. | qdrant-client PR #1247 review response (948d0ec) | unmined |
| OPS-8 | H | **I2V chain drift is a compounding failure, seams are not:** metric QA shows join deltas near zero (struct MAE 0.0027) while last-frame-vs-anchor color_L1 climbs 7.8→14.5 over 3 full segments — budget long clips by drift tolerance (ambient ≤3 seg, identity-critical ≤2), and use loop-mode (1/3 GPU cost, zero drift, motion reverses) for static shots. | CRE-3 chain-vs-loop QA (cre3_drift_qa) | unmined |
| OPS-9 | M | **Mode-switch teardown is a race:** agent-mode.sh slept 3s after stopping a ~29GB vLLM, but VRAM release lags → the next engine profiled into "KV cache -1.1GiB" and died. Fix = poll nvidia-smi for VRAM release (60s cap), never sleep a constant. | AI-5b shakedown; agent-mode.sh fix 2026-07-02 | unmined |
| OPS-10 | M | **Chat products: keep ALL policy in an engine + dumb transport adapters** — a mid-build Telegram→Discord swap cost ~nothing and a CLI adapter gave credential-free e2e the same night; and gate-by-construction beats gate-by-check (only owner-approved jobs can enter `queued`, so the GPU worker enforces approval with zero approval code). | creative production line (THIS-6) | unmined |
| OPS-11 | M | **Deterministic beats generative for status reporting:** the estate summary is fed to the digest LLM as an authoritative section AND appended verbatim — prompt discipline alone didn't survive a small model (it garbled "11 OK" into "Two OK" on the first wired send; the verbatim block carried the truth). Also: bash 5.2 `set -u` treats `${#arr[@]}` on a declared-but-never-assigned array as unbound — the all-green path was the untested one. | infra-digest estate wiring (FIX-6) | unmined |
| OPS-12 | M | **`build: .` containers drift silently from their repo** — rebuild+recreate after code changes; and without a .dockerignore, `COPY . .` bakes `.git` history and the live `.env` into a client-facing image. Doc defaults must also run cold: a README defaulting to an authed gateway with `LLM_API_KEY=not-needed` 401s for every stranger. | sovereign-legal-rag / rag-demo follow-ups | unmined |
| CN-11 | H | **A "residual list" is a snapshot, not a source of truth — re-verify against git before executing it.** The BIZ-9 list ordered a Track H install that git showed installed AND validated 06-13, then superseded by IndexTTS-2 the same day. Ten minutes of `git log` beat an evening of faithfully doing stale work. | comic-narrator BIZ-9 closeout | unmined |
| CN-12 | M | **"Need more voices" ≠ "need voice donors":** G8's male-voice gap was filled from 13 unused CC-BY-4.0 LibriTTS-R male speakers already on disk, curated by rerunning the bank's own median-F0 scan — pure CPU prep, one command from done. | comic-narrator G8 staging | unmined |
| OPS-13 | M | **Outward-facing counts drift fast:** proof-asset claims ("N PRs filed") were stale within 4 days of drafting — generate marketplace/résumé copy from one live-audited source (a gh query), never hand-copied numbers. | freelance docs housekeeping | unmined |
| OPS-14 | M | **A "stop everything" button gets clicked for "stop THIS thing" — offer the scoped stop.** Owner hit Mushishi Mode's StopAll to flush one vLLM and was surprised it swept the 8-container audio stack (by design, but no scoped alternative existed). Fix = new `stop-mode.sh` / "Stop Current Mode" row: stop only the primary GPU workload via the existing `stop_gpu_tenants_except <audio keep-set>` inverse — zero new stop logic. Bonus finds: audio-stop.sh printed "rq-dashboard preserved" while stopping it, and its restore hint predated coding-mode.sh — success/echo strings drift from the code they describe; test the *messages* too. | mushishi-mode v1.2.0 review | unmined |
| OPS-15 | S | **Outward copy is now generated, not written:** resume OSS section + marketplace PR counts regenerate daily from live gh status (opportunity-engine/oss-observer); edit oss-prs.json, never the text between the oss-auto markers. The first live audit caught drift both ways: the "11 PRs" claim included a closed-unmerged PR and the resume was missing 3 open ones. | oss-observer build | unmined |
| OPS-16 | S | **Publish the results, never the engine:** the sovereign-legal-rag showcase repo carries verbatim eval numbers, a live correct-refusal transcript, and a REDRAWN block-level diagram — model names, fusion mechanics, params, and prompts all stripped (grep-swept). Unverifiable claims omitted rather than repeated (a briefed number with no committed artifact stayed out). | sovereign-legal-rag-results build | unmined |
